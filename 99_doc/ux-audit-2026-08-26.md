# 操作性監査（2026-08-26）

操作の連鎖（この操作の後にこの操作をすると何が起きるか）を追い、利用者が迷う・誤解する・意図と違う結果になる箇所を洗い出したもの。
公開版（見やすい形）: https://claude.ai/code/artifact/e86473f1-f1b1-4817-ac8e-15da2255481c

> **対応状況（2026-08-27）**: 45件すべてに手を入れた。何をどう直したか・意図的に見送った点・
> 実機で確かめるべき点は `llm_doc/ux-audit-2026-08-26-fixes.md` を参照。
> このファイルは**指摘の記録**として当時のまま残す（行番号も2026-08-26時点のもの）。

## 調査方法

- コードを読んで想定した挙動を、ローカルHTTPサーバー経由（`http://127.0.0.1:8799/`、localStorage/IndexedDBが空の初回起動状態）でChromeから実際に操作して確認した。`file://` では未確認
- 「実機」= 画面と `canvas` の内部状態（`selectable` / `clipPath` / `stateStack` 等）を確認済み。「コード」= 該当コードを読んだ範囲での指摘
- 重大度: **高** = データが消える・戻れない・機能に到達できない / **中** = 迷う・手戻り / **低** = 違和感・不統一
- 行番号は 2026-08-26 時点の作業ツリー（main）のもの
- 件数: 高 11 / 中 22 / 低 12（計45）。実機 33 / コード 12

## 修正の優先順（根っこ）

個別の指摘は次の6つに集約できる。症状ごとではなく、ここを1か所ずつ押さえる方が修正漏れが減る。

| # | 構造 | 関連 |
|---|------|------|
| R1 | **「ロックが既定」なのにロックが見えない。** コマは `selectable:false` で生まれ、解除はレイヤーの鍵だけ。ナイフの出入りは全件を一律に書き換えて元の値を捨てる | #05 #06 #07 #12 |
| R2 | **モード状態が1か所に集まっていない。** ペン・トーン・ナイフは `ModeManager` を通らない経路があり、`#canvas-help-text` はクロップしか使わない。パネル切替はモードを解除しない | #10 #24 #25 #26 |
| R3 | **履歴の粒度が操作と一致しない箇所。** 名前・GUID付与やモード切替が「見えない履歴」を積み、Undoが空振りする。ボタンは端でも有効表示 | #08 #28 #29 |
| R4 | **ページは「保存された時」にしか登録されない。** 追加直後・起動直後のページは一覧に無く、番号・前後移動・消失の判定がそこから狂う | #32 #36 #11 |
| R5 | **親子リンクの解除経路が右クリックだけ。** `clipPath`（絶対座標）と `relatedPoly` は移動・削除・複製で付け直されず、解除は右クリックメニュー限定 | #13 #14 #16 #18 #31 |
| R6 | **後から差し込む文言が翻訳を通らない。** ラベル書き換え、テンプレートリテラルのUI、`textContent` 直書きが英日混在と切替漏れを生む | #21 #39 #42 #04 |

---

## 1. 起動・初回体験

### #01 [高][実機] 言語選択と自動保存復旧の2つのダイアログが重なって出る
- 操作: 前回作業を残して終了 → 言語未選択のブラウザで起動 → 「Select Language」の上に「Recover Auto-save」が被さる
- 期待: 先に言語を選び、その言語で「前回の続きを戻しますか」と聞かれる
- 実際: 復旧ダイアログ（`z-index:10000`）が言語一覧の中央を覆い、下の言語ボタンの一部が押せない。復旧ダイアログは言語未選択のため英語固定。「Don't recover」でその場で復旧データ削除
- 原因: `TutorialManager.init` と `AutoSaveManager.init → checkRecovery` が互いを知らずに同時にモーダルを出す。先に迫られるのが「消すかどうか」という後戻りできない方
- 根拠: `js/ui/tutorial.js:16-24`、`js/core/auto-save.js:144-200`（`showRecoveryDialog`、`discardBtn → clearAutoSave()`）

### #02 [高][実機] 初回セッションはAI基本設定欄が空欄のまま、内部の既定値で生成される
- 操作: 初回起動 → 右パネル「全体AI設定」（Prompt/Negative/Width/Height/Seed が空）→ コマを選んで Generate → 「masterpiece, best quality, 1girl, simple background」1024×1024 で生成
- 実際: `localStorage` に設定が無いと `loadSettingsLocalStrage()` が先頭で `return` し、`basePrompt` 既定値をUIへ書く処理に到達しない。内部 `basePrompt` は既定値のまま。設定自動保存が一度走ると次回から埋まるため初回セッション限定
- 根拠: `js/project-management.js:339-342`（`if(!stored)return;`）、同 `:355-363`、`js/core/settings.js:40`、`js/ai/prompt/base-event-listener.js`（changeでしか同期しない）
- 実機値: `basePrompt_prompt.value===""` かつ `basePrompt.text2img_prompt==="masterpiece, best quality, 1girl, simple background, "`

### #03 [低][実機] チュートリアルは5枚の説明を読むだけで操作させない
- 「次へ」で進むだけ。対象要素が無いステップは黙って飛ばす。再開導線は完了トーストの文言のみ
- 根拠: `js/ui/tutorial.js:98-110`、`runSteps / showStepHighlight`（`if(!targetEl){onNext();return;}`）

### #04 [低][実機] 起動と言語切替のたびに「SETTINGS LOAD」「WELCOME!」トーストが出る
- 言語を切り替えるたびに「HEY HEY! Welcome my friend!」が積まれ、本当のエラー通知が埋もれる（#41）
- 根拠: `js/ui/third/i18next.js:7502-7503`、`js/project-management.js:340`

## 2. コマ割り・ナイフ・コマ編集

### #05 [高][実機] コマは既定でロックされ、クリックしても選択枠が出ない。解除の入口はレイヤーの鍵だけ
- 操作: ひな形 → コマをクリック → ハンドルが出ない（`selectable:false`）→ ドラッグしても動かない → 鍵アイコンに気付くまで先へ進めない
- 実際: キャンバスは無反応だがレイヤー行だけ赤くなり、右パネルに「各AI設定（コマ）」が出る。半端な選択状態で、ロックが原因だと示すもの（カーソル・案内・トースト）が無い
- 根拠: `js/sidebar/panel/panel-manager.js:340, 379`（テンプレ読込時 `selectable:false`）、`js/layer/layer-button.js:301-318`（`putMoveLockButton` が唯一の切替UI）

### #06 [高][実機] 「コマ編集モードON」はロック解除＋選択が前提で、テンプレ直後に押すと空振り
- 操作: ひな形 → ページ管理「コマ編集モードON」→ 「パネル（コマ）を選択してください」→ コマをクリック（#05で選べない）→ 再度押しても同じ
- 実際: `Edit()` は `getActiveObject()` 無しで即return。順番「鍵を外す→コマを選ぶ→ON」はどこにも無い。入った後も `#canvas-help-text` は空、Escで抜ける旨も無い
- 根拠: `js/sidebar/panel/panel-manager.js:442-461`。前提を満たせばボタンは「コマ編集モードOFF」に変わりEscで解除されることは確認済み

### #07 [高][実機] ナイフモードを抜けると全オブジェクトのロックが外れる
- 操作: ひな形（全コマロック）→ ナイフON → Esc/モード解除 → 全コマ `selectable:true` → コマをクリックしたつもりでコマごとドラッグ、レイアウトが崩れる
- 実際: `canvas.forEachObject(obj.set({selectable:!isKnifeMode}))` で全件を一律に書き換え、元の値を覚えていない。`ModeManager.clearAll()` も全件 `selectable:true`
- 根拠: `js/ui/util/mode-manager.js:225-231`（`_updateMovement`）、`:363-385`（`clearAll`）、`js/sidebar/panel/knife/knife-mode.js:72-85`（`changeMovement`）

### #08 [高][実機] ナイフで1回切った分割が Ctrl+Z を3回押しても戻らない
- 操作: ナイフON（履歴+1: 名前/GUID付与）→ 分割（+2）→ Esc（+1）→ Ctrl+Z: 見た目が変わらない → ×2: まだ3コマ
- 実際: `stateStack` 差分でナイフON時点に `name/guid/guids` が付与され「見えない履歴」が積まれる。分割で2件、解除で1件
- 根拠: `js/sidebar/panel/knife/knife-split-engine.js:294-313`。実機: ON前 `stack=2` → ON後 `3`（差分 `obj.name/guid/guids`）→ 分割後 `5` → Ctrl+Z×3後も `polygon×3`

### #09 [中][実機] 分割後のコマ名が重複する（polygon1 / polygon2 / polygon1）
- レイヤーパネル、Ctrl+Pのプロンプト表示、ネーム一括生成のコマ番号で区別できない。ペンの線も `path8` が2本並び、終了時に `group8` へ統合（#26）

### #10 [中][実機] ナイフ・コマ編集・ペン・トーンのどのモードでもキャンバス上の案内文が空
- `#canvas-help-text` を使うのはクロップだけ。表示は「モード解除(ESC)」が赤くなるのみで、トーンでは赤くならない（#25）。`ModeManager.getCurrent()` もナイフ・ペン中に `'select'` を返す（ナイフはボタンから `changeKnifeMode()` を直接呼び `ModeManager.change()` を通らない）
- 根拠: `js/sidebar/panel/knife/knife-mode.js:9-16, 35-70`、`js/ui/util/mode-manager.js:243-251`（`hideCanvasHelpText` は crop.disable のみ）

### #11 [中][実機] 「縦ページ」「カスタムページ」「ひな形」「＋」で作られるページの中身と原稿サイズがそれぞれ違う

| 入口 | 置かれるもの | 原稿サイズ(mm) |
|------|------------|---------------|
| ページ管理「縦ページ」 | 全面コマ1枚 | 210×297（A4）に更新 |
| ページ管理「カスタムページ」(1380×4000px) | 何も無い | 更新されない |
| ひな形 | テンプレのコマ | 更新されない |
| ボトムバー「＋」 | 何も無い | A4固定 |
| 起動時既定 | — | 148×210（A5） |

- 根拠: `js/sidebar/panel/panel-template.js:120-121, 29-33`、`js/ui/bottom-bar.js:548-551`、`js/canvas-manager.js:33-35`。実機: カスタムページ後 `canvas=408×1183`、mm は `210×297` のまま

### #12 [低][コード] コマ設定スライダーは未選択時に無反応、「全てのコマ変更」は確認なしで即全件
- 根拠: `js/sidebar/panel/panel-manager.js:493-503`（`changePanelStrokeWidth`）、`:554-575`（`panelAllChange`）

## 3. 画像の配置とコマとのリンク

### #13 [高][実機] コマに入れた画像をドラッグしてコマの外へ出すと画像が消える
- 操作: 画像をコマにドロップ → 別コマへドラッグ → 元コマの外は描画されず選択枠だけ空中に残る
- 実際: `clipPath` が `absolutePositioned:true` で元コマの形に固定、`relatedPoly` も元のまま。乗り換えは無い。外す手段は右クリック「表示制限の削除」だけ（#31）
- 根拠: `js/sidebar/panel/panel-manager.js:150-207`（`placeObject → moveSettings`）、`js/core/util/fabric-util.js`（`updateClipPath`）、`js/ui/canvas-object-menu.js:232-236`。実機: 移動後 `top:577, clip:true, rel:'polygon1'`、画面上は何も見えない

### #14 [高][実機] コマを削除しても中の画像・トーンは残り、消えたコマの形で切り抜かれたまま
- 操作: コマに画像（とトーン）→ コマを選んでDelete → 枠線だけ消え画像は同じ形で残る → 動かすと存在しないコマの形で切れる
- 実際: `removeLayer()` は対象1件を `canvas.remove` するだけで `guids` の子に触れない。子の `relatedPoly` はキャンバスに無いオブジェクトを指し続ける。吹き出しは親子ごと消えるので同じ「親子」で挙動が分かれる
- 根拠: `js/layer/layer-management.js:533-544`。実機: コマ削除後、子の Tone が `clip:true`、`relatedPoly` がキャンバス外の参照

### #15 [中][コード] ドロップ先のコマは外接矩形で判定するため、斜め・重なりコマでは意図と違うコマに入る
- `findTargetFrame()` は `getBoundingRect()` にポインタが入るかで判定し最前面から最初に当たったコマを返す
- 根拠: `js/sidebar/panel/panel-manager.js:215-232`

### #16 [中][コード] OSクリップボードから画像を貼り付けると「選択中のオブジェクト」の中へ入れようとする
- `paste` 処理は選択があればその中心へ `putImageInFrame(img,x,y,…,activeObject)`。テキストや吹き出しでも `targetLayer` として渡される。未選択ならキャンバス左上1/4に置かれ、コマへ入れる操作は右クリック「panelIn」のみ
- 根拠: `js/shortcut.js:365-401`、`js/sidebar/panel/panel-manager.js:150-162`

### #17 [中][実機] 非表示にしたオブジェクトが選択されたまま残りハンドルだけ見える
- `visible:false` でも `selectable/evented` は `true`、選択も維持。Deleteで見えないものが消える
- 根拠: `js/layer/layer-button.js:172-177`

### #18 [低][コード] Ctrl+C/V の複製はコマとのリンクもGUIDも持たず +10px ずらして置かれる
- `clone()` は `commonProperties` を通らない。`clipPath` は元コマ位置の静的コピーで、動かすと #13 と同じ症状
- 根拠: `js/shortcut.js:104-118`、`llm_doc/history-and-data.md`

## 4. テキスト・吹き出し

### #19 [中][実機] 吹き出しの既定文言が「203x126」で、置いた直後に選択が外れる
- 操作: ひな形💭 → 左上に「203x126」の吹き出し、選択は外れる → 本体クリック → 文字をダブルクリックでやっと編集
- 実際: 横書きの既定文言は内接矩形の寸法 `${w}x${h}`、縦書きは "new"。末尾で `discardActiveObject()`。置き場所 `left:50` 固定で既存テキストに重なった
- 根拠: `js/sidebar/speechBubble/speech-bubble-text.js:241-246`（寸法文字列）、`:239`（縦書き "new"）、`js/sidebar/speechBubble/speech-bubble-effect.js:173-191`

### #20 [中][実機] 横書き／縦書きテキストは常に (50,50)・14px に置かれ、大判キャンバスでは見つけにくい
- 836×1183px のキャンバスで 28×16px の "New" が左上。選択中のコマには置かれず、リンクも無い。2回押すと重なる
- 根拠: `js/sidebar/text/text-effect.js:266-268`、`js/sidebar/text/vertical-text.js:11-12`、`js/sidebar/text/text-2-manager.js:30-33`（画像テキストは20pxずつずらして8回で一巡）

### #21 [中][実機] オブジェクトを選ぶとサイドバーのスライダー名が英語に化ける
- 「線幅:」「不透明度:」「サイズ」「アウトラインサイズ」が、コマやテキスト選択の瞬間に「Line Width：2.4」「Opacity：100」「Size：14」「Outline Size：0」に変わり戻らない。値を反映する処理がラベルごと英語で書き直していると推定（**該当箇所は未特定**）
- 根拠: 実機（ページ管理・テキストパネル）。`base-ja.js:544, 589, 631, 633` には日本語訳がある

### #22 [中][コード] 自由💭の座標モードは「4点以上打って始点付近をクリック」で確定するが画面に無い
- 操作: 座標モード → 点を打つ → 終わらせ方が分からず Esc/選択モード → 打った点が全部消える
- 確定条件 `points.length>=4 && isNearStartPoint()`。Escは `sbClear()` で破棄。同パネルに「不透明度」スライダーが2本（`sbFillOpacity` / `sbFillOpacity2`）
- 根拠: `js/fabric/fabric-management.js:512-530`、`js/sidebar/speechBubble/speech-bubble-freehand.js:464-500`、`index.html:1297-1303`

### #23 [低][コード] 文字装飾カードの効き先（選択中と、次に作るテキスト）はツールチップにしか無い
- 根拠: `index.html:1355`（`data-tip="textDecorCardHint"`）、`js/sidebar/text/text-effect.js:289`

## 5. モード遷移（ペン・トーン・効果）

### #24 [中][実機] ペンを選んだまま別パネルを開いても描画モードが続く
- 操作: ペン→鉛筆 → サイドバー「テキスト」を開く → キャンバスをドラッグ → 鉛筆の線が引かれる
- `toggleVisibility()` はパネル表示を切り替えるだけで `ModeManager` に触れない
- 根拠: `js/sidebar/sidebar.js:5-43`。実機: `canvas.isDrawingMode===true` のまま

### #25 [中][実機] トーンは Esc でも「モード解除」でも終わらず、同じプリセットをもう一度選ぶしかない
- トーンを選ぶと即座にコマ（未選択なら全面）へ置かれ、スライダーごとに作り直し。「モード解除」は点灯せず Esc も効かない（`clearAll` にトーン終了処理が無い）。終了方法はカードのツールチップのみ
- 根拠: `js/sidebar/tone/tone-manager.js:46-66`、`js/sidebar/tone/tone.js:167-194`、`js/ui/util/mode-manager.js:363-385`

### #26 [中][実機] ペンで引いた線はモードを抜けると1つのグループに統合される
- `path8`×2 が Esc で `group8` 1件に。1本だけ消す・動かすができず、事前に示されない
- 根拠: `js/ui/util/mode-manager.js:269-292`（`pencil.disable → finalizeGroup`）

### #27 [低][実機] ズーム倍率の表示が無く、Ctrl+8/9/0 の割当は F1 でしか分からない
- 根拠: `js/shortcut.js:11-13`、`js/canvas-manager.js:236-260`

## 6. 履歴・レイヤー・ショートカット

### #28 [中][実機] Undo/Redo ボタンが常に押せる見た目で、効かない状態が分からない
- `#undo/#redo` は `disabled` にならない。#08 の不可視履歴と重なり「押したのに何も起きない」と「もう戻れない」の区別が付かない

### #29 [中][実機] ページを離れた Undo は効かず、履歴に残らない操作があると Undo が「1つ前の操作」ごと消す
- 履歴はページ単位（仕様）。加えてドラッグ移動直後の Ctrl+Z で移動ではなく配置そのものが取り消された場面が1回（2回目は正常。**再現条件未特定**）。矢印キー移動は正常
- 根拠: `js/fabric/fabric-management.js:331-370`

### #30 [低][コード] ショートカットの効く条件が種類ごとに違い、Ctrl+S/Ctrl+O/Alt+N はメニューに表記が無い
- Undo/保存系は `isEditableTagsActive()`（INPUT/TEXTAREA/DIV/SELECT）を見るが、Ctrl+C/V・Delete・矢印・Esc はライブラリ既定のフィルタ任せ。DIVにフォーカスがあると Ctrl+Z だけ効かない
- 根拠: `js/shortcut.js:51-64, 104-135, 400-410`、`index.html:354-370`

### #31 [低][コード] 右クリックメニューにしか無い機能があるのに右クリックの存在は案内されない
- 「表示制限の削除」「切り抜き」「複製」「panelIn」「設定資料」は右クリックが唯一/主な入口。チュートリアル・ヘルプ・F1に記載なし（拡張機能経由ではメニュー表示は確認できず、DOM内容のみ確認）
- 根拠: `js/ui/canvas-object-menu.js:151-260, 671-683`

## 7. ページ・保存・出力

### #32 [高][実機] ページ追加直後、新ページはボトムバーに載らず「1/1」表示、Alt+← も効かない。空のまま移動すると消える
- 操作: 1ページ目を描く → 「縦ページ」→ ボトムバーが開くが新ページのサムネイル無し「CLOSE 1/1」→ Ctrl+Z → Alt+←: 何も起きない → サムネイルで1ページ目へ → 新ページは登録されず消える
- 実際: ページは `btmSaveProjectFile()` が走った時にしか `btmProjectsMap` に登録されない。未登録は `btmGetGuidIndex()` が -1 でハンドル表示・Alt+←→ が成り立たない。中身が無いと `btmShouldSaveCurrentPage()` が偽でページごと消える。起動直後の1ページ目も60秒後の自動保存か別操作まで一覧に出ない（Ctrl+B を開くと空）
- 根拠: `js/ui/bottom-bar.js:28-56, 58-65, 107-117`、`js/sidebar/panel/panel-template.js:9-45`。実機: 追加後 `btmProjectsMap.size===1`、現GUID未登録、Alt+← 無反応、戻った後も `size===1`

### #33 [高][コード] プロジェクト読み込みは現在のページを置き換えず後ろに追加する（確認なし）
- 操作: 作業中A（3ページ）→ 読み込みB（5ページ）→ A3枚＋B5枚 → Ctrl+S → 混ざったファイルが保存
- 実際: 現ページ保存後 `multiLoadLz4()` で各ページを `btmAddImage()` するだけ。`btmProjectsMap.clear()` は自動保存復旧と効果一括復元でしか呼ばれない。未保存警告は `beforeunload` のみで、読み込み・全削除・設定リセットの前には出ない
- 根拠: `js/project-management.js:64-140`、`js/core/compression/project-compression.js:215-260`、`btmProjectsMap.clear` 呼び出し元: `js/core/auto-save.js:205`、`js/sidebar/effect/effect-batch.js:352`、`js/ui/bottom-bar.js:332, 362`、`js/core/unsaved-guard.js`

### #34 [中][コード] 保存ファイル名が常に「DESU-Project.lz4」で、保存すると自動保存データが消える
- 保存直後 `AutoSaveManager.clearAutoSave()`。保存後の作業がクラッシュすると次の自動保存まで復旧不可
- 根拠: `js/project-management.js:49, 55`

### #35 [中][実機] 原稿サイズ(mm)は出力サイズの「下限」でしかなく、カスタムページでは A4 指定のまま 2480×7195px が出る
- `getCropAndDownloadLink()` は mm×DPI とキャンバス比から `Math.max` の倍率を取る。mm は「これより小さくならない」下限。メニューは「原稿サイズ」「DPIダウンロード」としか書かれず、ピクセル幅(1380)との関係も読めない
- 根拠: `js/core/util/image-util.js:405-427`。実機: `canvas 408×1183`、mm 210×297、DPI 300

### #36 [低][実機] ボトムバーは「OPEN (Ctrl+B)」だけで、複数ページが作れること・どこで追加するかが分からない
- 開いても空（#32）。追加は「＋」（ページが無いと存在しない）か左サイドバー「ページ」
- 根拠: `index.html:1992-2000`、`js/ui/bottom-bar.js:229-236`

## 8. AI設定・LLM・表示言語

### #37 [高][実機] 「使用モデル・ワークフロー設定」は ComfyUI 未起動なだけで「ノード不足→Install Missing Custom Nodes」を出す
- 全ノード「未確認」、中央に「Downで保存→ComfyUIへドロップ→Install Missing Custom Nodes」、右端に「ComfyUIに接続できません」が同時表示。生成側（`checkWorkflowNodeVsComfyUI`）は言い分けるよう直されているが、エディタ側の照合表示は未取得＝全ノード欠落のまま
- 根拠: 実機（`ComfyUI (Local) OFF`）、`llm_doc/ai-system.md` ObjectInfo の節

### #38 [中][実機] 生成AI設定ウィンドウは Esc で閉じない（他のダイアログ・ピッカーは閉じる）
- 閉じる手段は×か暗幕クリックのみ。保存ボタンが無く即時反映で、閉じた時に保存されたか迷う
- 根拠: `js/ai/ui/unified-settings-window.js:13-16`、`index.html:2022`

### #39 [中][実機] 使用サービス表・コマ別AI設定・コントロール・トーン名が英日混在で誤記もある

| 場所 | 表示 |
|------|------|
| 使用サービス表（日本語UI） | Text → Image / Image → Image / Inpaint / 高解像度化 / 背景削除 / Image → Image Angle / CLIP / **DEEPDOORU** / 文章を英語プロンプト化 / 画像からプロンプト化 |
| 右パネル「各AI設定」 | Width(-1=Use Base) / Seed(-1=**Rundom**, -2=Use Base) / Generate（英語固定、i18n未対応） |
| コントロールパネル | Angle / Scale / Top / Left / SkewX / SkewY / Opacity |
| トーンのピッカー | Tone / ToneNoise / Tone Snow / SpeedLine / FocusingLine |
| Ctrl+P プロンプト表示 | 「Prompt / nothing」 |

- 根拠: `js/ui/ai/auto-prompt-ui.js:28-43, 128`、`js/ai/role/ai-roles.js:68-77`（`roleInterrogateDEEPDOORU`）、`js/sidebar/panel/panel-manager.js:698`

### #40 [中][実機] LLM未割り当てで「プロンプトを作る」を押すと、設定への導線が無いトーストだけが出る
- ストーリーが空でも同じトーストが先に出る（入力チェックより前）。次の一手（生成AI設定→Grok/Ollama にチェック）が本文に無く `storyPromptStatus` も空。エラートーストは10秒残り押すたびに積まれる
- 根拠: `js/ai/prompt/llm/llm-story-ui.js:41, 253-259`

### #41 [中][実機] トーストが右下に積み上がり、右パネル下部（Generate等）を覆う
- エラー10秒・歓迎5秒・情報4秒が同列に積まれ、5枚で「各AI設定」の Width/Seed/Generate が隠れる。ホバーでカウントダウンが止まるため触るほど消えない
- 根拠: `index.html:2004`、`js/ui/toast.js`（`startProgressBar`）

### #42 [中][実機] 言語を切り替えても設定資料ウィンドウの案内文（7か所）は前の言語のまま
- 「同じ人物・同じ背景で描かせるための資料です…」「まだ付けていません」「一覧からここへ落として付けられます」「まだ登録がありません」「画像を追加」「画像をドロップ（複数枚まとめて）」「このウインドウのどこへ落としても入ります」が `textContent` 直書きで `updateContent()` の対象外
- 根拠: `js/ai/reference/reference-sheet-window.js:460-466, 481-485, 535-541`

### #43 [低][コード] 「設定値自動保存」OFFでもロール割り当てだけは自動保存される
- ラジオで `applyAssignments → debouncedSettingsSave()` が無条件に走り全設定（空欄含む）を保存。URL/APIキーはOFFで保存されない
- 根拠: `js/ai/role/role-assignment-ui.js:21-28`、`js/project-management.js:479-497`

### #44 [低][コード] ダッシュボードの「全てクリア」だけがブラウザ標準の confirm()
- 根拠: `js/dashboard/dashboard-ui.js:161, 172, 183`

### #45 [低][実機] 色ピッカーの入力欄が狭く「rgba(25」で切れる
- ページ管理「塗り」、キャンバスメニュー「キャンバス背景」など jscolor の入力欄

---

## 未確認・保留

- ドラッグ移動直後の Ctrl+Z で「移動」ではなく「配置」が取り消された現象（1回だけ再現。2回目は正常）
- Ctrl+C 直後の Ctrl+V が無反応（`clone()` が非同期のため連打では `_clipboard` 未設定と推定。人の操作間隔では起きない可能性が高い）
- 右クリックメニューの表示（拡張機能経由では `display:none` のまま。DOM の中身は確認済み）
- #15（外接矩形判定）、#16（貼り付け先がテキスト）はコード上の指摘で実機未確認
- #21 のラベルを英語で書き換えている箇所はコードから特定できていない

## 確認できた、迷わない作りになっている点

- ページ削除・全削除・設定リセットは内容を列挙した確認ダイアログを通り、×・Esc・背景クリックが揃っている
- ComfyUI 未接続で Generate を押した時の文面は原因と対処が読める
- ペン・トーン・文字装飾・画像テキストのピッカーは名前と一言説明が付き、Esc／外クリックで閉じる
- テキスト編集（ダブルクリック→入力→Esc→Delete）と、吹き出し削除の親子連動・Undo復元は期待どおり
- 画像テキストは「選ぶ」と「挿入」が分かれ、未選択で挿入すると理由が出る
