# 吹き出し輪郭のチェックリスト

`llm_doc/review-checklist.md` から分離したもの。
スーパー楕円・オフセット多角形・周期ノイズ・しっぽなど、
**吹き出しの輪郭を生成する処理を触るときだけ**読む。他の作業では読まない。

## チェック項目

| # | カテゴリ | チェック内容 | 回数 |
|---|---------|-------------|------|
| 71 | 描画 | SVGを`<img>`経由でラスタライズする処理で、ページ側のフォントに依存していないか。`<img>`のSVGは別ドキュメント扱いで`document.fonts`に足したFontFaceも外部URLのフォントも見えず、OSに入っているフォントだけが名前解決できる。使えないフォントを選ばせると黙って別の字体で描かれる  | 1 |
| 123 | 幾何 | 媒介変数の角度を、**中心から見た角度（極角）として使っていないか**。スーパー楕円・楕円は細長いほど両者がずれ、700x100 の吹き出しでしっぽの付け根が指す方向から最大36.7度ずれて生えていた。**点列には必ず `atan2` で測った極角を持たせる** | 1 |
| 124 | 幾何 | 輪郭を太らせる／細らせる処理で、**オフセット量が形の細かさを超えていないか**。点の間隔が線幅を下回ると内外のオフセットが交差し、`evenodd` の塗りに穴が開く。**点数を線幅で頭打ちにし、オフセット量を隣の辺の長さの45%で頭打ちにする**。尖った先端は1点で尖らせず、線幅ぶんの短い平らを作る | 1 |
| 129 | 幾何 | 閉じた輪郭にしっぽ等を差し込むとき、**差し込んだ部分どうしの交差も見ているか**。しっぽを曲げたら本体とは交差しないのにしっぽの左右が交差し、塗りが破綻した。判定を「差し込み部分 対 本体」だけにすると見落とす。**差し込み部分 対 全辺**で見る | 1 |
| 132 | 幾何 | 輪郭を内側へオフセットするとき、**指定した距離が実際に出ているか測ったか**。`offsetPolygon` は交差を防ぐため「ずらす距離を隣の辺の長さの45%で頭打ち」にしており、点の間隔5pxの輪郭に8pxを指定しても2.2pxしか動かず、**二重線が輪郭の帯に埋まって1本にしか見えていなかった**。しかも見た目は「線が少し太い」だけなので気づけない。**大きくずらすときは何回かに分けてずらし、結果の距離を数値で確認する** | 1 |
| 134 | 幾何 | オフセットで折り返した部分を、**向きの反転だけで拾おうとしていないか**。「元の辺と逆向きになった辺を落とす」方法では、離れた辺どうしが重なる交差（しっぽの付け根・角落としの角）が残り、最大132px²の穴が開いていた。**交差を実際に探して短い側のループを切り落とす**（グリッドに辺を入れれば点数の2乗にならない）。ただし短い側が全体の1/4を超えるときは形そのものの破綻なので触らない | 1 |
| 137 | 幾何 | 「差し込む形」の寸法を、**差し込む先の大きさから決めていないか**。しっぽの根元の幅を本体の平均半径から決めていたため、しっぽの長さとは無関係に太さが決まり、実測で `長さ/根元幅` の平均 0.77・**根元幅が長さを上回るケースが83%**になっていた（しっぽではなくイボに見える）。**差し込む形は、その形自身の寸法どうしの比で決める** | 1 |
| 138 | 幾何 | 左右対称であるべき形を、**片側 `a-b`・もう片側 `a+b` のように非対称にずらしていないか**。しっぽの曲げをこの形で入れていたため、曲がるのではなく捻れて見えていた。**曲げるなら軸そのものを曲げ、幅は軸に垂直に取る** | 1 |
| 139 | 幾何 | 「近い方を選ぶ」処理で、**探す向きを限定したか**。しっぽの根元をトゲの頂点へ寄せる処理が前後どちらも探していたため、手前側の根元が奥側へ回り込んで左右が入れ替わり、輪郭が交差してしっぽが 1632通り中166件も省略されていた。**左右・前後の区別がある処理は、探索範囲もその側だけに限る** | 1 |
| 140 | 幾何 | 補正を**必要な領域から効かせているか**。細長い楕円が笹の葉に見えるのを防ぐためスーパー楕円の指数を上げていたが、縦横比1.15から効かせていたので縦横比2.4で**周長の26%が直線**になり、楕円ではなくカプセルに見えていた。実際に笹の葉になるのは縦横比4以上。**「効かせすぎ」は「効かない」より気づきにくい**（形が壊れず、別の形に見えるだけ） | 1 |
| 141 | 幾何 | 揺らぎを載せる前に、**点の間隔を揃えたか**。媒介変数を等間隔に取ると、輪郭上の点の間隔が場所によって 7〜10倍 変わる（実測）。そのまま添字で揺らぎを載せると、片側だけ細かく震えて反対側は滑らかになり、CGに見える。**弧長で取り直してから載せる** | 1 |
| 142 | 幾何 | 揺らぎを載せる向きを、**輪郭の性質に合わせたか**。法線方向に載せるとトゲの斜面で点が横に動き、中心から見た極角の順序が壊れる（実測で18ケース147箇所）。極角の順序を前提にした処理が下流にあるなら**半径方向に載せる** | 1 |
| 143 | 幾何 | 零平均の周期ノイズだけで「手描き感」を出そうとしていないか。**零平均の揺らぎからは「傾き」も「片側だけ膨らむ」も原理的に出ない**。骨格そのもの（回転・テーパ・中心のずれ）を崩す必要がある | 1 |
| 145 | 数値 | 設定値と**実効値がずれていないか**。`periodicNoise` は次数ごとの重み `1/(k+1)` をそのまま足すので、3本で合計が指定値の1.83倍になっていた。0.08 のつもりで調整すると効きが約1.8倍ずれる。**足し合わせる係数は正規化して、設定値＝実効値にする** | 1 |
| 147 | 検証 | 輪郭に対する「入るか」の判定で、**縁を十分細かく拾ったか**。1辺16点だと点と点の間で輪郭が食い込む形（花形の深い谷）を見落とし、はみ出しが残る | 1 |
| 148 | 検証 | 測る側と描く側で、**クランプ（頭打ち）の条件まで揃えたか**。`build` 側だけ線幅を `平均半径×0.14` で頭打ちにしていたため、太い線を指定すると測ったときと描いたときで輪郭が変わり、文字がはみ出していた。seed だけでなく**全ての派生値**を揃える | 1 |
| 149 | 検証 | 「縁を細かく拾えば大丈夫」で済ませていないか。矩形の辺を等分しても**角度の等分にはならない**。角度幅1度の谷は、辺を48等分しても長辺の中央（1点0.6度）で跨いで見落とす。**比が単調になる区間の境目だけを見れば、点を増やさずに厳密になる**（この場合は輪郭の頂点の角度と矩形の角の角度） | 1 |
| 150 | 幾何 | **弧長で取った位置の「幅」を、弦で測っていないか**。トゲや深い谷の輪郭では弧長が山谷を辿るぶん長くなり、弦は最大1/6まで縮む。狙った幅にならず、長さ92pxに対して根元4pxの紐ができていた。**弦が目標になるまで弧長を伸ばす** | 1 |
| 157 | 描画 | 折れ線で曲線を描くとき、**点の間隔を線幅より細かくしたか**。間隔が線幅を上回ると角がそのまま見えて線がジグザグになる。揺らぎも点の位置でしか変わらないので、粗いほど角張る。「細かく刻むと帯が痩せる」なら、痩せる原因（オフセットの上限）の方を直す | 1 |
| 158 | 描画 | 高周波のノイズを**輪郭を細かくしたあとで見直したか**。粗い点列では見えなかった振幅が、細かくすると数珠つなぎの丸として現れる。線幅に対する振幅は ±0.16 程度まで、波長は線幅の7倍以上 | 1 |
| 159 | 幾何 | 上限を外すとき、**代わりの上限を用意したか**。「隣辺長の45%」を外して幅を正しくした一方、折り返しを止めるものが無くなり、後処理が点を削って**長い弦＝太い塊**になっていた。正しい上限は「その点で折り返さない限界 = (min(l1,l2)/2)/tan(θ/2)」で、真っすぐな所には掛からない | 1 |
| 161 | 描画 | 先細りの形を**線形補間で作っていないか**。根元から先端へ直線で結ぶと幅が一定の割合で細り、稲妻や筆致が「先の尖った割れ目」に見える。中心線と細り方を分け、細り方に指数を掛けて根元の太さを保つ | 1 |
| 163 | 描画 | 周期ノイズの次数を**上へ**重ねていないか。`[k, k*1.6, k*2.3]` のように積むと、狙った波長帯の 1/3 まで短い波が混ざる。中程度のうねりは既に短波長側の端なので、これで細かい波が折れ線に見えていた。**次数は1つずつしか離さない**（`[k, k+1, k+2]`）か、下へ重ねる | 1 |
| 164 | 検証 | 「滑らかさ」を目視だけで判断していないか。**隣り合う辺の向きの差（折れ角）**を測れば数字で出る。この件は 中央2.47度・最大29.0度 → 中央0.92度・最大3.2度 と、直したかどうかが一目で分かった。目視だけだと「直した」と誤認する | 1 |
| 165 | 検証 | 自己交差のループの大きさを測るとき、**交点で分かれる2つの弧のうち短い方**を取ったか。長い方を取ると、面積 0.36px² の無害なループが 32,783px² と出て「重大な破綻」に見える |2 |
| 166 | 描画 | 線の滑らかさを**中心線で測って満足していないか**。実際に塗られるのは「中心線 ± 幅」の帯の縁で、隣り合う点で幅がわずかに変わるだけで縁が折れる。中心線の折れ角が中央1.5度でも、帯の縁は中央4〜6度・最大37度になっていた。**測る対象は描かれるパスの方** | 2 |
| 167 | 描画 | 幅に載せる高周波ノイズの波長を、**点の間隔で表せる範囲に収めたか**。振幅 A・波長 λ・点の間隔 h のとき、帯の縁の折れ角はおよそ `A(2π/λ)²h`。これが目標角以下になる λ を式で決める。波長が点の間隔の4〜6倍しかないと、線を太くするほど折れが目立つ（線幅10で5度超が56%） | 1 |
| 168 | 描画 | 周期的に変わる量の**折り返し点を段差にしていないか**。インク補充を「切れた値から満タンへ一気に」戻していたため、一周に1〜3箇所だけ線幅が飛んで「節」に見えていた。線が太いほど目立つ。**一周の数%を立ち上がりに使って繋ぐ**。値を周期ごとに変えていると繋がらないので、その場合はまず値を1つに揃える | 1 |
| 173 | 描画 | 「接線連続にした」と書いた継ぎ目を、**実際に出た点列の折れ角で測ったか**。3次ベジエの制御点を接線方向に置いても、腕が短く分割が粗いと1つ目の標本が制御点の方へ振れ、点列としては折れる（実測: 腕を根元幅の0.06倍にしていたため 24.7〜56度）。腕の長さと分割数の両方を上げて初めて 6.5度になった | 1 |
| 174 | 描画 | **経路ごとに点の間隔が違っていないか**。同じ吹き出しの中で本体1.9px・しっぽ5.67px、角のある輪郭12px、連結13.3px と3種類の規則が混在していた。粗い側だけが「角張る」「揺らぎが棘になる」「帯の縁が出っ張る」を同時に起こす。**間隔を決める規則は1箇所に集約する** | 1 |
| 175 | 幾何 | 隣の辺の長さに比例する上限を使うとき、**長さ 0 に近い辺が混ざらないか**。しっぽの根元は輪郭の辺の内分点なので元の点と 0.0003px しか離れないことがあり、`(min(l1,l2)/2)/tan(θ/2)` がそこで 0 に落ちて**帯が1点だけ閉じた**（＝線に白い切り欠き）。オフセットの前に、辺の長さの中央値を基準に短すぎる点を落とす | 1 |
| 176 | 描画 | 先端の「切り口」の幅を、**そこの線幅と比べたか**。切り口が線幅より広いと、閉じた輪郭の内側に白い隙間が残り「割れた蹄」に見える。切り口 ≦ そこの帯の幅 にすれば左右が重なって必ず閉じる | 1 |
| 177 | 幾何 | ベジエの制御点が**軸を越えていないか**。くびれを根元幅に対する比で入れていたため、短くて太いしっぽで左右の制御点が入れ替わり、太さ0の紐になっていた（実測で左右の距離 0.0012px）。ベジエは制御点の凸包に入るので、**制御点を同じ側に残せば左右が交わらないことを保証できる**。比は「その位置での幅」に対して取る | 1 |
| 178 | 数値 | 周期的に変わる量を、**名目の周期**で正規化していないか。補充位置にジッタを入れているのに `直近からの距離 × 回数` で正規化していたため、間隔が名目より短い区間では値が下限まで下がりきらず、境目で1点だけ飛んだ（実測: 小声で 1.061 → 0.000）。**実際の区間の長さで割る** | 1 |
| 179 | 数値 | 平滑の窓の大きさが、**平滑したい量の大きさに追従しているか**。窓を「周長の1.2%」と「線幅の0.4倍」で決めていたため、窓÷線幅が線幅2で2.95倍・線幅25で0.40倍と必要な向きと逆に動いていた。太い線ほど段差が目立つ症状は、ここでも起きる | 1 |
| 180 | 数値 | ノイズの**振幅だけを線幅に比例させていないか**。波長（や幅）を定数で頭打ちにすると、傾き＝振幅÷波長が線幅に比例して増える。実測で中心線の最小曲率半径が線幅25のとき 128.9px → 63.8px に落ちていた。**下限に当たるときは振幅の方を落として傾きを保つ** | 1 |
| 183 | 検証 | 「線が太いほど目立つ」を確かめるとき、**線幅を1〜10まで振って同じ指標を取ったか**。中央値は改善しているのに線幅10だけ悪化する、という形で残ることが多い | 1 |
| 184 | 幾何 | 形を変えずに**点を細かくしただけ**で壊れる式が無いか。`(min(l1,l2)/2)/tan(θ/2)` のように「隣の辺の長さ」に比例する上限は、真っすぐな辺を刻むと刻み幅に引きずられる。**向きが変わるまでの長さ**で測れば刻み方に依存しない。実測で四角の角の折り返しが 3箇所 → 35箇所 → 7箇所と動いた | 1 |
| 185 | 描画 | オフセットの**閉じる側にマイターを掛けたか**。オフセットした2直線の交点は開く側も閉じる側も二等分線上の `d/cos(θ/2)` にある。閉じる側だけ `d` のままだと交点に届かず、角の点が戻りきらずに切り欠きと棘が出る（実測: 機械音声の折り返しが 51→32） | 1 |
| 187 | 描画 | 曲線を点列にするとき、**等間隔が常に良いとは限らない**。3次ベジエを弧長で等間隔にすると曲率の高い区間が相対的に粗くなる。媒介変数 t で等分すると制御点の腕が短い側に点が集まり、結果として曲率に追従する（実測: 帯の縁の出っ張り 0.3px超が 弧長1.90% / 媒介変数0.91%） | 1 |
| 189 | 数値 | 走査窓の**最小値**を取る処理で、窓を広げすぎていないか。周期的に落ちる量（歯・弁ごとに落ちる曲率の上限）に対して、窓が周期より広いと**全域が谷の値になる**。平滑の窓と最小値の窓は別に決める | 1 |
| 190 | 検証 | 指標が px なのか角度なのかで**結論が逆になっていないか**。しっぽの根元は、点を細かくしたことで1点あたりの折れ角は 74.9度→156.7度に増えたが、実際の突起は 2.63px→1.73px に減っていた。稲妻の肩でも、窓の中の**向きの総変化**は 109.7度→229.0度と悪化する一方、最大の突起は 14.48px→11.16px と改善した。**角度は「分散させただけ」と「本当になめらか」を区別できるが、実害に近いのは px の方**。両方見て、どちらを採るかを書き残す | 2 |
| 191 | 幾何 | 太さのある線で描く形を作るとき、**すきまが線幅より狭い場所を作っていないか**。輪郭が交差していなくても、通路が線幅を下回れば内側の縁は必ず重なる。交差判定では拾えない。実測で 3.67px のすきまに 8.45px の帯を通そうとして 274px² の塗り残りが出ていた | 1 |
| 192 | 幾何 | すきまの検査で、**近くて当たり前の組まで見ていないか**。先細りの左右、接続部の前後は必ず近い。除かないと 1632通り中 893件が弾かれた。「輪郭の上で十分離れた組」だけを見る | 1 |
| 193 | 描画 | 2本の線を並べて描くとき、**間の白が縁の揺らぎで埋まらないか**。間隔から2本の出しろを引いた残りが、帯の縁に載せたノイズの振幅を下回ると合体する。実測で線幅10の二重線の隙間が 0.29px しか残らず、振幅 1.65px のノイズで埋まっていた。**片方の太さを隙間から逆算する** | 1 |
| 195 | 検証 | 指標が**ユーザーの見ているスケール**を測っているか。「頂点と両隣の弦との距離」は波長が点の間隔の数倍の高周波しか拾わない。指摘された「数珠」は波長20〜70pxのなだらかな幅のうねりで、この指標では**ずっと0に近いまま**だった。指標を「幅を弧長に並べて局所的な山の出っぱりを数える」に変えて初めて、通常・線幅10 で幅が 3.50〜14.85px を一周に11回行き来していることが出た。**「直っていない」と言われたら、まず指標がその症状を測れるかを疑う** | 1 |
| 196 | 検証 | 症状の**要因を1つずつ外して数えたか**。数珠は5つの要因のうち内外エッジの独立ノイズだけがコブの数を決めており（11個→3個）、残る4つは1つも変えなかった。全部を少しずつ調整すると、効かない側をいじって「良くなった気がする」で終わる | 1 |
| 198 | 検証 | 自己交差の**件数だけで良否を判断していないか**。今回は 101→159箇所と増えたが、うち154箇所は面積0.25px²未満で塗りに出ない。塗りとして見えるループは 8件→1件、最大面積は 65.4px²→2.1px²。**面積の分布で並べる** | 1 |
| 201 | 数値 | 記号として必要な変化を、**乱数の係数まかせ**にしていないか。かすれの深さを `dip×(0.6〜1.3)` で決めていたため、係数が1を下回った seed（約6割）では幅が一度も0にならず、小声が「細い楕円」にしかならなかった。**記号が成立する最低限は割合で確保し、乱数は残りに掛ける** | 1 |
| 202 | 描画 | **意図した変化を平滑の中に入れていないか**。インクの切れを幅の平滑の内側で掛けていたため、切れが均されて埋まっていた。既になめらかな関数なら平滑の後に掛ける。切れ区間を足しても本数が 5.2本 止まりだったのはこれが原因 | 1 |
