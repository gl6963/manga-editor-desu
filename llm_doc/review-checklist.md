# レビューチェックリスト

ユーザーから修正ポイントの確認を求められた際に見落とされた項目を蓄積する。
同じ問題が繰り返される場合は回数をインクリメントして優先度の目安とする。

**上限は設けない。** 過去の指摘を消すと同じ見落としが再発するため、件数では削らない。

## チェック項目

| # | カテゴリ | チェック内容 | 回数 |
|---|---------|-------------|------|
| 4 | UI | 既存の表示・表記と統一されているか  | 0 |
| 5 | fallback | エラー時にfallback値で動作を続行していないか（fallback禁止）  | 2 |
| 7 | ログ | console.logではなくSimpleLoggerを使っているか。デバッグ時もlogger.trace等を使いログレベル変更で対応する  | 1 |
| 8 | 命名 | 変数名がcamelCaseか、APIレスポンスのキーを変換していないか  | 0 |
| 10 | GUID | 新規オブジェクトにguidを付与しているか  | 0 |
| 11 | イベント | EventDelegatorのパターンに従っているか（直接addEventListenerしていないか）  | 0 |
| 12 | 履歴 | ループやcanvas.add/remove連続呼び出しでsaveStateByManual()が都度発火しないか。都度発火すると大量の履歴スナップショットが生成され、メモリの圧迫・履歴保存による画面の固まり・Undo/Redoで無意味な操作が発生する。複数操作はwithoutHistory(fn)で囲み最後に1回だけ保存（同一タスク内のイベントは自動集約されるが、非同期をまたぐ処理は集約されない）  | 1 |
| 13 | 性能 | forループ等でオブジェクトのプロパティを連続変更する際、毎回canvas.renderAll()を呼んでいないか。都度呼ぶとオブジェクト数×ループ回数分の再描画が走りUIがフリーズする。ループ外で最後に1回だけ呼ぶ  | 1 |
| 14 | ログ | Errorオブジェクトを文字列化する際にJSON.stringifyを使っていないか。Errorのmessage/name/stackは非列挙プロパティのためJSON.stringifyでは`{}`になる。`instanceof Error`で判定し`error.name + error.message`等で展開する  | 1 |
| 15 | ログ | 各ファイルで`new SimpleLogger()`していないか。ロガーは`js/core/logger.js`に集約定義し、各ファイルではグローバル変数として参照する  | 1 |
| 16 | 非同期 | `_comfyUIExecProvider`等のグローバル変数に依存するURL/認証情報を、長時間のawait（WebSocket待機等）をまたいで使っていないか。非同期処理中に別タスクがグローバル変数を上書きし、別プロバイダのURLに接続してしまう。関数冒頭でサーバーアドレス・認証情報をローカル変数にキャプチャして使う  | 2 |
| 17 | fallback | グローバル変数が未設定のときデフォルト値（`'local'`等）にフォールバックしていないか。`_comfyUIExecProvider`がnullのときキーを`'local'`にする、`comfyObjectInfoListMap.get(key)\|\|comfyObjectInfoListMap.get('local')`のように別キーにフォールバックする等はすべて暗黙のfallback。awaitなしで呼ばれたasync関数ではグローバル変数が既にクリアされているため、呼び出し元で値を事前キャプチャして引数で渡す  | 1 |
| 18 | 履歴 | オブジェクト属性を`obj.left=100`のように直接代入するUIハンドラで`commitHistory()`/`commitHistoryDebounced()`を呼んでいるか。canvasイベントが発火せず、自動コミット網（`_set`フック）も直接代入は検知できないため履歴に残らず、次のUndoが1つ前の操作まで巻き戻る。連続入力（スライダー・キーリピート）は必ずdebounce版を使う  | 1 |
| 19 | 履歴 | `changeDoNotSaveHistory()`の解除漏れがないか。非同期コールバックをまたぐ場合はtry/finallyで囲む。解除漏れは以降の全操作が履歴に残らなくなり、Undoが大きく巻き戻る。同期処理は`withoutHistory(fn)`を使う  | 1 |
| 20 | 履歴 | 一時的なUI用オブジェクト（クロップ枠等）に`setNotSave`/`excludeFromLayerPanel`/`excludeFromExport`を設定し、`addByNotSave`/`removeByNotSave`で追加削除しているか。通常のadd/removeでは無意味な履歴が積まれる  | 1 |
| 21 | 履歴 | 画像を差し替える効果（glfxフィルタ等）で、非同期の完了後にコミットしているか。`setElement()`は`set()`を通らないため自動コミット網で検知できず、抑止スコープも非同期コールバックには効かない。プレビューのまま履歴に残らないと次のUndoが1つ前の操作まで巻き戻る  | 1 |
| 22 | 保存 | プロジェクトファイルに新しいjsonを追加するとき、履歴状態の読み込み対象に混入していないか。状態判定は`state_`プレフィックスで行う。読み込み側は`loadLz4BlobProjectFile()`と`generation-task-manager.js`の2箇所あり、片方だけ直すと裏側のAI処理でだけ壊れる  | 1 |
| 23 | ページ | ページ切り替え・削除・追加・auto-saveで`isProjectBusy()`を確認しているか。読み込み中に保存すると、読み込み途中のキャンバスを現在ページのblobに上書きしてページが壊れる  | 1 |
| 24 | ページ | ページを離れる前に`flushHistory()`しているか。デバウンス中の変更が確定する前に離れると変更が失われる  | 1 |
| 25 | i18n | 動的生成するダイアログの文言をハードコードしていないか。`innerHTML`で組み立てる場合も`getText()`を通す  | 1 |
| 26 | AI | 設定ウィンドウを開かないと初期化されない処理に依存していないか。ComfyUIのObjectInfoは設定ウィンドウを開くまで取得されず、起動直後は生成・背景削除が一律に失敗していた。必要な時点で取得すること  | 1 |
| 27 | AI | ロール判定（`hasNotRole`）に渡すロールが処理内容と一致しているか。T2IボタンにImage2Image、I2IボタンにText2Imageが指定されていた  | 1 |
| 28 | 非同期 | `while(true)`の監視ループ内で例外を握りつぶしているか。握りつぶさないとループごと終了し、以降その監視が二度と動かない  | 1 |
| 29 | UI | 選択オブジェクトの値をパネルに反映しているか。反映しないと表示中の古い値が残り、スライダーを少し動かしただけで別の値に飛ぶ。`syncObjectControls()`を使う  | 2 |
| 30 | UI | jscolorのピッカーを`value`代入で更新していないか。見た目が変わらないため`picker.jscolor.fromString()`を使う  | 1 |
| 31 | UI | `oninput`のハンドラが対象と一致しているか。吹き出しの色入力に`changePanelStrokeColor`が指定されており、吹き出しには無反応でコマ選択中はコマの線色が変わっていた  | 1 |
| 32 | UI | 条件を満たさず処理しないとき、画面を空欄のまま残していないか。理由を表示しないと操作ミスか処理中か判断できない。**操作の手順そのものも同じ**で、リファレンスシートは「コマを選んでから一覧を押す」という順番をどこにも書いておらず、一覧を眺めたまま止まる作りだった  | 2 |
| 33 | file:// | 実行に必要な外部ライブラリを`third/`に同梱しているか。CDN参照はオフラインで失敗する  | 1 |
| 34 | 性能 | 一覧を全件まとめて生成していないか。ブレンドは25モード分のプレビューを毎回生成していた。開いている範囲だけ生成する  | 1 |
| 35 | UI | 画面座標へ変換するとき、表示倍率を状態変数ではなくDOMから実測しているか（`getCanvasDisplayScale()`）。CSSのscaleと状態変数がずれると位置が狂う  | 1 |
| 36 | UI | はみ出し判定をビューポート基準で行っているか。キャンバス基準だと拡大時にキャンバス右端が画面外でもクランプされず、要素が画面外に出る  | 1 |
| 37 | UI | スクロール対象の要素を取り違えていないか。`overflow:hidden`の要素に`scrollLeft`を代入しても何も起きない  | 1 |
| 38 | UI | スクロール領域内で`transform:scale()`する要素の`transform-origin`が`top left`か。中央基準だと左と上のはみ出しに到達できない  | 1 |
| 39 | 保存 | 破壊的操作のバックアップにauto-saveを使っていないか。auto-saveは`pages`/`metadata`の**単一スロットを毎回上書き**し世代を残さない。スキップ判定も表示中ページの履歴状態ハッシュ基準のため、操作後は必ず走って潰れる。**復元導線は起動時ダイアログのみで、任意のタイミングで押せるボタンは存在しない**。戻せる必要があるなら専用ストアへ退避し復元ボタンを用意する  | 1 |
| 40 | 性能 | ループで回す処理の中で`fx.canvas()`のようなWebGLコンテキストや`texture()`を毎回新規生成していないか。コンテキストはブラウザ上限（16程度）で古いものがロストし途中から結果が壊れる。textureはGPU上に残り解放されない。1個を使い回し`loadContentsOf()`で詰め替える  | 1 |
| 41 | 履歴 | 非同期をまたぐ一括処理で`withoutHistory(fn)`を使っていないか。`withoutHistory`は同期関数用でawaitを待たないため抑止が先に解除される。`changeDoNotSaveHistory()`〜try/finally〜`changeDoSaveHistory()`を使う  | 1 |
| 42 | エラー | 一括処理で1件の失敗を握りつぶして次へ進んでいないか。どこまで適用されたか分からなくなる。打ち切って「何件目まで完了したか」を必ず表示する。呼び出し元がawaitしないasync関数はcatchを持たせないとunhandled rejectionになり、オーバーレイだけ消えて無言で終わる  | 1 |
| 43 | ページ | 「中身があるか」の判定を履歴件数（`stateStack.length>=2`）で代用していないか。キャンバスのリサイズや背景色設定だけでも履歴は積まれるため、起動直後の空白ページがページとして保存され、ボトムバーに空サムネイルが増える。判定は`btmShouldSaveCurrentPage()`に集約し、キャンバス上のオブジェクト有無で判断する  | 1 |
| 44 | ページ | 空判定でキャンバス上のUI専用オブジェクト（初期メッセージ`isInitMessage`等）を除外しているか。除外しないと空ページでも「中身あり」と誤判定する。ボトムバーだけ除外して他を直し忘れると片方だけ壊れる。空判定は`getContentObjectCount()`に寄せ、`getObjectCount()`を白紙判定に使わない  | 2 |
| 45 | ページ | `chengeCanvasByGuid()`のawait後にキャンバスの中身を数えていないか。内部の`lastRedo()`→`applyHistoryState()`は`canvas.loadFromJSON()`をコールバックで呼ぶだけで完了を待たないため、awaitが返った時点では**オブジェクトが0件**。エラーも例外も出ず「ページが切り替わるだけで何も起きない」になる。`isProjectBusy()`がfalseになるまで待つ  | 1 |
| 46 | 既存API | 既存関数を書き換えるとき、旧名で呼んでいる箇所を全部追ったか。`C2BWStartLight`等を消したのに`effect-manager.js`が呼び続けており、選択画像スコープがReferenceErrorで無反応になっていた。`grep -rn "旧関数名" js/`で確認する  | 1 |
| 47 | 仕様確認 | そのアプリで「ロック」「非表示」等が実際にどのプロパティで表現されているか確認したか。このアプリのロックは`selectable`で、`lockMovementX`等ではない。fabricの一般的な属性を当て推量で使うと条件が永久に成立しない  | 1 |
| 48 | i18n | `data-i18n="[title]xxx"`のような属性翻訳記法を使っていないか。`updateContent()`は`element.innerHTML = translation`しかせず属性に対応していないため、キー文字列がそのまま本文に差し込まれ`material-icons`のリガチャが壊れる。属性の説明文は`data-tip`を付けて`addTooltipsByAttribute()`（tippy）に載せる  | 1 |
| 49 | UI | サイドバーパネルの`left_area`に`style="display:none"`を付けたか。付け忘れるとDOMContentLoadedで`toggleVisibility()`が走るまでパネルが開いた状態で描画される  | 1 |
| 50 | UI | 一覧と設定を同じ縦列で場所を奪い合わせていないか。パネル幅200pxに一覧と操作盤を同居させると、一覧に`max-height`を付ければ二重スクロール、付けなければ設定が画面外に押し出される。一覧は内容分だけ、設定は残り全部という配分にする  | 1 |
| 51 | UI | 押した後の挙動が違うボタンを同じ見た目で並べていないか。効果は即適用・設定後Submit・プレビュー付き・別ウィンドウの4通りがあり、見分けが付かないと結果が予測できない。グループ見出しと印（`preset-item-mark`）で押す前に示す  | 1 |
| 52 | UI | 適用範囲のような「対象を選ぶUI」を、それが効かない項目まで含む位置に置いていないか。効果パネルの適用範囲は変換グループにしか効かないため`preset-section`の囲みの内側に入れる  | 1 |
| 53 | i18n | `innerHTML`で後から差し込んだ`data-i18n`要素を翻訳したか。`applyLabelTranslations()`は`data-i18n-label`専用で`data-i18n`を見ない。差し込み後に`updateContent()`を呼ぶ  | 1 |
| 54 | i18n | 他要素から表示名を写している要素を、言語切替後に取り直しているか。`updateContent()`は`data-i18n`を持つ要素しか更新しないため、写した値は前の言語のまま残る  | 1 |
| 55 | 重複定義 | 呼ぼうとしているグローバル関数が複数ファイルで定義されていないか。`updateContent`は`i18next.js`とprompt-helper内に同名で存在する（後者はクロージャ内なので非グローバル）。`grep -rn "function 関数名" js/`で確認する  | 1 |
| 56 | 保存 | その退避は本当に永続化が必要か検討したか。「直前の操作を取り消す」だけならメモリ上の変数で足り、リロードで消えて構わない。永続化するとプロジェクトごとのスロット管理・世代上限・掃除の導線が芋づるで必要になり、消し忘れたデータがブラウザに残り続ける。永続化しないと決めたらツールチップ等でユーザーにも明示する  | 1 |
| 57 | 保存 | 複数プロジェクトを跨ぐ保存で、プロジェクトを識別しているか。固定キー1スロットだと別プロジェクトを開いている間も復元でき、押した瞬間に現在のプロジェクトが別プロジェクトの内容で丸ごと置き換わる。このアプリはページGUID（`canvasGuid`）が`commonProperties`経由でプロジェクトファイルに保存されるので識別に使える  | 1 |
| 58 | UI | 選択オブジェクトの反映処理で「種類が同じなら何もしない」分岐になっていないか。同じ種類でも中身は別物のため、値を入れ替えないと前に選んでいたオブジェクトの値が表示され続ける。アアアを選んだあとイイイを選んでもサイドバーがアアアのままだった  | 1 |
| 59 | 保存 | オブジェクトに持たせる設定スナップショットを「利用者が触った項目だけ」から作っていないか。`sidebarValueMap`は変更のあったidしか持たないため、未変更の項目が欠けたスナップショットになり、選び直したときにその項目だけ前のオブジェクトの値が残る。DOMの入力要素から集める  | 1 |
| 60 | UI | 「選ぶ」操作が生成・実行まで行っていないか。押すたびに既定位置へ新規作成する作りだと、同じ座標に重なって増えるため見た目が変わらず、履歴とレイヤーだけが増える。選ぶ操作と挿入操作を分ける  | 1 |
| 61 | DOM | 利用者の入力をテンプレートリテラルで`<textarea>`の中身として埋め込んでいないか。`</textarea>`を含む入力でDOMが壊れる。描画後に`element.value`へ代入する  | 1 |
| 62 | 仕様確認 | 実現手段を決める前に、同じ目的の既存機能を探したか。「プロンプトを設定する機能」に対し、既に`prompt/llm/`のLLM生成基盤と「ネームから一括生成」があるのに手入力欄を作ってしまった。同じ領域に既存実装がある場合はまずそれに載せられないか検討する  | 1 |
| 63 | i18n | チェックボックスやアイコンを含む要素に`data-i18n`を付けていないか。`updateContent()`は`innerHTML`ごと差し替えるため、中の`<input>`や`<i>`が消える。内側に`<span data-i18n>`を置く  | 1 |
| 64 | i18n | 後から差し込んだDOMに翻訳を当てる処理が、`document`全体を走査して`textContent`で上書きしていないか。フォント管理モーダルとImage Prompt Helperがこれをやっており、開いた瞬間に他モジュールのボタンまで巻き込んで`&#128295; 使用モデル・ワークフロー`のように実体参照が生のまま表示されていた。走査は挿入したコンテナ配下に限定し、`updateContent()`と同じく`innerHTML`で入れる  | 1 |
| 65 | 保存 | API取得後に`<option>`が作られるselectを、保存値の復元対象にしていないか。`el.value=保存値`はマッチするoptionが無いと無言で`''`になり、設定の自動保存が全項目を一括保存するため**localStorage側の保存値まで空で潰れる**。Grokのモデル選択がリロード後に復帰しなくなっていた。復元は`applySettingValue()`（option補完付き）を使い、一覧の再取得側も取得失敗時に選択値を空にしない  | 1 |
| 66 | AI | モデル一覧の自動取得条件を「ロール割り当て済み」だけにしていないか。APIキーだけ入れてロール未割り当ての状態では一度も取得されず、設定ウィンドウのモデル欄が空・disabledのまま「再取得」を押すまで直らない。設定ウィンドウを開いたときは接続情報が入っているプロバイダを`{silent:true}`で取得する  | 1 |
| 67 | 非同期 | 長時間動く一括処理の待ちに`requestAnimationFrame`や`setTimeout`のポーリングを使っていないか。非表示タブではrAFが一度も発火せず処理がそこで永久に止まり、setTimeoutも1秒〜1分間隔まで間引かれる。`waitNextFrame()`を使い、完了待ちは通知で起こす（`TaskQueue.whenIdle()`）。タイムアウト判定からは隠れていた時間を除く  | 1 |
| 68 | UI | 一覧の項目で名前と説明を縦に積んでいないか。説明の行数が項目ごとに変わるとグリッドの同じ行が高い方に揃えられ、`overflow:hidden`と重なって見本画像が切れる。1行に並べて`text-overflow:ellipsis`で詰め、全文は`title`で読めるようにする  | 1 |
| 69 | UI | 一覧項目を`<button>`で作るとき、中身を自前のflex/gridで並べているか。`<button>`の中身はブラウザ既定で無名ボックスに包まれ、グリッド項目として高さを引き伸ばされたときの配置が読めない。`display:flex`を明示する  | 1 |
| 70 | UI | 項目数や文言の長さを増やしたあと、実際の表示を確認したか。9件で成立していたレイアウトが31件・長い説明文で崩れた。件数を増やす変更はレイアウト変更でもある  | 1 |
| 72 | 性能 | 作り直されるコンテナの中でコンポーネントを`new`していないか。`switchText2Ui()`は`innerHTML`ごと差し替えるため、その中で`new FontSelector()`するとインスタンスと`document`リスナーが選択のたびに積み上がる。作り直されない場所に1つだけ置く  | 1 |
| 73 | 読み込み順 | 他ファイルの`const`をトップレベルで参照していないか。`defer`スクリプトは記述順に実行されるため、定義側が後ろにあると読み込み時点でReferenceErrorになる。関数の中からの参照は実行時なので順序に依存しない  | 1 |
| 74 | 根拠 | 「なぜそれが効くのか」を、確認していない仕組みで説明していないか。**LLMが「前から取って止める」から並び順が要る**（LLMはそんな動作をしない）、**Danbooruの件数＝タグの効き目の強さ**（件数が示すのは学習されているかとモデルの既定値だけ。効き目はタグの意味の狭さで決まる）、**3000トークンで指示追従が劣化する**（検索で出たブログの、長文入力の推論性能の話を確認せずに適用した）。もっともらしい機構を作って設計の根拠にすると、そこから派生する仕様が全部間違う。確認できないなら「効果は未検証」と書く  | 3 |
| 75 | LLM | プロンプトに具体的なタグ・単語を例として並べていないか。LLMは「使うべきものの一覧」と受け取り**全出力に入れる**。`buildings, sky, road`と書けば室内の場面に`sky`が入り、効果線を列挙すれば寄りのコマ全部に効果線が入り、`{"index":1,"role":"establishing"}`という出力例を書けば1コマ目がestablishingに固定される。判定は「そのタグが大半の出力に出たとき、意図通りか壊れているか」。例を出すなら件数付きの参照表にして「選択肢ではない」と明記する。**一度塞いでも再発しやすい**  | 2 |
| 76 | LLM | プロンプトに根拠のない固定値を書いていないか。「最大6キャラ」「1コマ15〜40タグ」「4コマ以上なら人物なしを1枚」。しかも上限は**超過分を黙って捨てる**ためfallback禁止に反する（7人目のキャラがどこにも表示されず消える）。上限ではなく「何を作る対象とするか」の判断軸を渡す  | 1 |
| 77 | LLM | プロンプトに書くタグが実在するか確認したか。`biting lip` `hand over mouth` `teary eyes` `arms crossed` `sweating profusely` `hand out of frame`はDanbooru 0件、`downcast eyes`は202件で効かない。**存在しないタグは効かないだけでなく、効いているつもりになるので害がある。** `https://danbooru.donmai.us/tags.json?search[name_comma]=tag1,tag2`で件数を確認する  | 1 |
| 78 | LLM | プロンプトが文学的な言い回しになっていないか。「読者が手に持って帰るもの」「握った拳と真顔のほうが怒鳴り顔より怒って見える」では何をすればいいか分からない。**読んでそのまま手が動く形**にする。判断させる項目には「何を見て決めるか」と「外したら何が起きるか」を必ず添える（→ `prompt-composition.md` 判断軸の章）  | 2 |
| 79 | LLM | システムプロンプトとユーザーメッセージで違うことを言っていないか。システム側を「フレームに入る範囲だけ取れ」に変えたのに、ユーザーメッセージのラベルが`copy these tags verbatim`のまま残り、顔だけのコマにも靴が入っていた。手順に「2/3〜3/4に置く」と固定で書いた直後に「転から始めてもよい」を足して矛盾させた。**片方を直したら、同じことを言っている全箇所を`grep`で洗う**  | 2 |
| 80 | 説明 | 表にまとめて要約していないか。セルに収めると各項目が結論だけになり、何が起きているのか読み取れない。**実際の入力・実際の出力・実際に送っている文字列をそのまま並べる。** 動かして取った値を使い、書き直さない  | 1 |
| 81 | LLM | プロンプトを「こうすべき」の断定で書いていないか。**判断の余地があるものを決め打ちで書くと、毎回同じ出力しか出ない。** 「見せ場の直前に静かな場面を置く」「起承転結の4つがどこかに存在しなければならない」では、全部の話が同じ構成になる。選べる手を並べ（`・A … / ・B … / ・C …`）、外したときに何が起きるかを添える形にする。断定してよいのは出力形式・モデルの挙動・アプリの制約だけ  | 1 |
| 82 | LLM | プロンプトの言語が混ざっていないか。英語で書いた指示を日本語のUIから使うと、翻訳調の読めない文が残り、どこが指示でどこが例か分からなくなる。**プロンプトは日本語で書く。** 英語のままにするのはDanbooruタグそのものと出力フォーマットの記号だけ  | 1 |
| 83 | ドキュメント | 既存のドキュメント（README・functionList等）を流用・引用する前に、実装と照合したか。`functionList.html`はフォント29種/フキダシ35種/保存Zip形式/対応言語9種/対応APIはSD WebUIのみ、READMEは保存`.json`形式と、いずれも実装と食い違っていた。古い記述を土台にすると誤りがそのまま新しい成果物へ複製される  | 1 |
| 84 | 調査 | サブエージェントに調査を任せるとき、除外フォルダ（`third`等）に結論の根拠が入っていないか。`third/GoogleAnalytics/googleAnalytics.js`がGTMコンテナ本体だったため、除外指示に従ったエージェントが「解析は送信されていない」と誤報告した。公開する事実は依頼者自身が裏取りする  | 1 |
| 85 | 公開 | 公開ページ・READMEにプライバシーに関する断定を書くとき、実装で確認したか。「データはどこにも送信されない」はGoogleアナリティクスとクラウドAI（Fal.ai/Grok/RunPod）の双方で事実と異なっていた。APIキーが平文でlocalStorageに入る点も明記が必要  | 1 |
| 86 | 保存 | オブジェクトの`strokeWidth`を変えたあと`refreshInitialStrokeWidth()`で`initial`も更新したか。`resizeCanvas()`は再フィットのたびに`initial.strokeWidth`から線幅を計算し直し、`initial`は追加時の1回しか記録されない。**履歴の復元も再フィットを通るため、Undoした瞬間に縁だけ消える。** 既存の「アウトラインサイズ」スライダーでも起きていた  | 1 |
| 87 | 保存 | 複製でも残したいプロパティを`commonProperties`だけに足していないか。`commonProperties`は`canvas.toJSON()`にしか効かず、`obj.clone()`（コピー＆貼り付け）は通らない。対象クラスの`toObject`側に足す  | 1 |
| 88 | UI | 一覧の行で、幅のほとんどを占める入力欄がクリックを止めていないか。レイヤーパネルは行全体が選択のあたり判定なのに`.layer-name`が`stopPropagation`しており、サムネイルの付かないテキストレイヤーは押して選択できる場所が行に残っていなかった。入力欄は`readOnly`＋`mousedown`の`preventDefault()`でフォーカスを取らせず、クリックは行へ通す  | 1 |
| 89 | UI | 作り直されるパネルで、編集中かどうかをDOM側に持たせていないか。レイヤーパネルは選択のたびに`innerHTML=""`で全部作り直し、しかもデバウンスで遅れて走るため、入力欄のフォーカスや`readOnly`は編集開始直後に消える。編集中の行はGUID等で覚えて作り直しの最後に戻す。**作り直しで消えるときも`blur`は飛ぶ**ので作り直し中フラグで区別し、**作り直しの先頭でフォーカスが離れていたら畳む**（畳まないと以後ずっとフォーカスを奪い続け、キャンバス側の文字入力ができなくなる）  | 1 |
| 90 | UI | 作り直される行に`dblclick`を付けていないか。1回目のクリックで行が作り直されると2回のクリックの対象が別物になり、dblclickは行ではなく共通の祖先へ飛ぶ。素早く2回押したときだけ動く不安定な操作になる。作り直されない入れ物で受け、対象から辿れないときは`elementFromPoint()`で引き直す  | 1 |
| 91 | UI | 入力欄が表示している値と、入力を書き込む先が同じか。レイヤーパネルはテキストレイヤーの行に本文（`layer.text`）を出しながら入力を`layer.name`へ書いており、打った文字が作り直しのたびに本文へ戻っていた。既定値を別の項目から引くなら、編集したかどうかを覚えて同期を止める  | 1 |
| 92 | UI | 対象を選んでから使うウインドウをモーダル（暗幕付き）にしていないか。暗幕がキャンバスを覆うと対象そのものを選べず、1件ごとに開き直すことになる。**一覧から対象へ次々に適用する道具はフローティングにする。** 位置・大きさを覚え、開くたびに画面内へクランプする  | 1 |
| 93 | UI | 一覧の項目を「対象が選ばれていない」という理由だけで薄くしていないか。リファレンスシートは対象なしで全カードを`opacity:0.45`にしており、モーダルで対象を選べない＝**常に全部暗い**状態だった。薄くしてよいのは「押しても入らない」ことが確定している項目だけ  | 1 |
| 94 | UI | サムネイルを暗い下地＋`object-fit:cover`で置いていないか。透過のある素材は黒くつぶれ、縦長・横長の素材は切れて中身が読めない。市松の明るい下地＋`contain`で全体を出し、細部が要るものには拡大表示を付ける  | 1 |
| 95 | UI | 直す頻度の高い項目（取り込み直後の名前など）を「編集」ボタンの中に隠していないか。全件を直す作業では1枚ごとに2回押すことになる。入力欄は常時出し、**縦に積んで壁になるのは表示モード（大/小/一覧）で避ける**  | 1 |
| 96 | 性能 | 開きっぱなしのウインドウで、選択が変わるたびに一覧を全部作り直していないか。`data:`URLのサムネイルを枚数分読み直すことになり、キャンバスを押すたびに一覧がちらつく。**並んでいる顔ぶれの署名を持ち、同じなら状態だけ当て直す。** 状態で変わる見た目は1か所（`applyCardState()`）に集め、組み立て側に状態を持たせない  | 1 |
| 97 | DnD | 一覧に並べた`img`を既定のまま置いていないか。`img`はブラウザ既定でドラッグでき、Chromeはそれを**ファイルとして**落とすため、自前のドロップ口が反応して**動かしただけで同じ絵が2枚になる**。掴ませたい親を`draggable`にして`img`は`draggable=false`にし、ドロップ口は自分の中から始まったドラッグを受けない（独自のMIMEで見分ける）  | 1 |
| 98 | UI | ロックした対象（`selectable=false`）に、選択を通してしか設定できない作りにしていないか。リファレンスの割り当ては`canvas.getActiveObject()`だけを対象にしていたため、コマ割りを固めてロックした後は**ロックを外す→付ける→掛け直す**しか手が無かった。一覧から対象へドロップする等、選択を経ない経路を用意する。**別経路を足しても、元のクリックが壊れたままになっていないか確認する**：ロックした対象を押すとfabricは`selection:created`を出さず、代わりに`selection:cleared`を出す。選択イベントだけを見ていると押すたびに対象が空になり、送る順が読めない。`mouse:down`（選択の確定後に飛ぶ）で押した位置から対象を引き直し、当たり判定はドロップ側と同じ関数に寄せる  | 2 |
| 99 | UI | フローティングウインドウの大きさを右下1か所でしか変えられなくしていないか。画面の右端・下端に寄せると掴み口が画面の外に出て、そこから広げられなくなる。4辺と4隅に置き、左・上を掴んだときは反対側の辺を動かさない  | 1 |
| 100 | AI | 複数の参照画像を同時に送る仕組みで、**参照どうしの関係を表す手段**を用意したか。リファレンスシートは1コマにAさん・Bさん・武器を同時に付けられたが、「その武器が誰のものか」を伝える行がどこにも無かった。本文はDanbooruタグの羅列（`2girls, holding sword`）なので**タグ側でも属性の付け先を書けない**。関係は**コマ単位**で持ち（シートの説明文に書くと同じシートを使う全コマに乗る）、**番号ではなくidで結ぶ**（`Image N`は組み立てのたびに数え直すため、自由記述の番号は並べ替えで別の画像を指す） | 1 |
| 101 | DnD | ドロップの受け口を、カード1枚分の大きさのタイルだけにしていないか。リファレンスシートは登録0枚のときも追加タイルがカード1枚分しかなく、隣の広い空白の方が目に入って落としても素通りしていた。**受け口は実際にはウインドウ全体だったが、それが枠にも文字にも出ていなかった。** 空のときは受け口を一覧の幅いっぱいに広げ、どこまで落とせるかを文字で出す。複数枚まとめて受けられるなら落とす前に枚数を出す（`dataTransfer.files`はdrop時まで読めないが、`items`の件数はdragover中に読める） | 1 |
| 102 | UI | 同じ対象を別の言い方で何度も出していないか。リファレンスシートはバーに「対象コマ」、帯に「このコマの参照」、チェックに「前のコマを参照」と3通りの「コマ」が並び、どれが今の対象なのか読み手が探すことになっていた。**対象の呼び名は1つに決め、他の見出しは対象を指し直さず役割だけを書く**（「送る順（1枚目から）」など）。効きが未検証のまま足した機能は、分かりにくさの分だけ損になるので消す | 1 |
| 103 | UI | 機能の名前をカタカナの外来語のまま置いていないか。「リファレンスシート」は字面から何をするものか読めず、**現場に通じる言葉（設定資料）** があった。名前を変えるときは**8言語すべて**と周辺語（ベース→共通の資料）まで揃える。コード側の名前（`sheet` / `base`）は変えず、画面とコードの対応表をドキュメントに残す | 1 |
| 104 | UI | 機能の入口を専用ウインドウの中だけにしていないか。キャンバス上の表示切り替えが設定資料のウインドウにしか無く、ウインドウを開かないと切れなかった。メニューにも置く。**状態は1か所（`setEnabled()`）が持ち、チェックの同期もそこから一括で行う。** 入口ごとのonchangeで相手を触ると、入口を増やすたびに直す場所が増える | 1 |
| 105 | CSS | **他所のCSSの素の要素セレクタ**（`css/form.css`の`span{color:var(--text-color-B);margin:0 3px}`、`select{width:100%}`等）を踏んでいないか。(1) flex行の項目が`flex-basis:auto`のまま親の幅を取って1行ずつ改行される→`width:auto`を明示する。(2) **ボタンの文字を`<span>`で包むと、そこだけ地の文字色になり隣のボタンと色が変わる**（生成AI設定の3つのボタンで、バッジを持つ「設定資料」だけ`<span>`に入れていた）→ラベル用のクラスを付けて`color:inherit;margin:0`を当てる。バッジのように自前の色を持つ子は巻き込まない | 2 |
| 106 | DnD | `dropEffect`に`effectAllowed`へ入れていない値を書いていないか。dragstartで`effectAllowed='link'`にして、dragoverで`dropEffect='move'`（外す側）にすると、Chromeはdropを**黙って捨てる**。dragstart→dragover→dragendまで来るのにdropだけ来ないため、掴めて運べるのに落ちた瞬間に何も起きない。両方使うなら`'linkMove'`のように許可側へ両方入れる。**「ドラッグは始まるのに反映されない」ときは最初にここを見る** | 1 |
| 107 | DnD | 掴める場所を絵で出しているか。行のうち一部（サムネイルと名前）だけを`draggable`にすると、`cursor:grab`はポインタを載せるまで出ないため、どこを掴むのか分からず「ドラッグできない」で終わる。取っ手（`drag_indicator`）を置き、案内文でも掴む対象を名指しする（「ドラッグで〜」ではなく「サムネイルを〜へドラッグ」） | 1 |
| 108 | UI | もの同士の**つながり**を、文字と選択欄で説明していないか。設定資料の関係は「1資料1行＋関係先プルダウン＋矢印」で出しており、(1)矢印がどちらの所属を指すのか読めない (2)関係を付けていない資料の行まで同じ見た目で並んで壁になる (3)送る順の帯と同じサムネイルが2か所に出て二重に見える、の3つが同時に起きていた。**つながりは画像で読める形にする＝枠で囲み、同じ枠に入っていれば関係がある。** 1つだけの枠は囲みを消し、枠と枠の間は枠の中より広く取る | 1 |
| 109 | UI | 同じものを見せる場所を2つ作っていないか。「送る順」と「資料どうしの関係」は**どちらも付けた資料の一覧**で、並べ方が違うだけだった。片方に情報を足すほど二重に見える。**1か所にまとめ、順番は番号、組は枠**のように軸を分ける。持ち方も1つにする（枠が正で、送る順は枠から作り直す） | 1 |
| 110 | UI | 数字を出すとき、**利用者の判断に効く単位**で出しているか。設定資料は送信量をKBで出していたが、画像生成で効くのは料金も上限もトークン側で、KBは判断材料にならなかった。単位を変えられないときは、効く方を主に出し、元の値はツールチップへ回す。**推定値は`~`と「目安」を付け、根拠（数え方）をドキュメントに残す** | 1 |
| 111 | DnD | 掴めるものと落とせる場所を**総当たりで確認したか**。設定資料は枠へスロットを落とせるのに、一覧のカードだけ受け付けず「コマへ落とすかクリック」しか無かった。受け口ごとに`draggingXxx`を直接見ると足し忘れる。**掴んだ中身を1つの関数で取り出し（`dragPayload()`）、受け口はそれを渡すだけ**にする | 1 |
| 112 | UI | 一覧で直せる項目を、**実際に使う場所でも直せる**ようにしたか。設定資料の名前は一覧のカードでしか直せず、送る順の側では読むだけだった（しかも幅が狭く名前が切れていた）。その場で直せない項目は、直すたびに目と手が往復する。ドラッグする要素の中に入力欄を置くときは、`mousedown`で`draggable`を切り`blur`で戻す | 1 |
| 113 | DnD | 並びに差し込むドロップで、**列の末尾**へ入れる手があるか。設定資料は「サムネイルの手前へ入れる」しか無く、一番右へ足すには枠の細い余白を狙うしかない上に印も出なかった。**左半分＝手前 / 右半分＝次**で受け、どちらに入るかを左右どちらかの縁で示す。受け口を作ったら**印も必ず作る**（光らないと落ちたのか素通りしたのか分からない） | 1 |
| 114 | データ | 「1つの対象に同じものを2回」使う場面を潰していないか。設定資料はコマ内でidを一意にしていたため、**Aさんに紐づけたラーメンをBさんにも紐づけられなかった**。組（枠）の中だけ一意にし、組をまたぐ重複は許す。送る実体は重複させない（送る順は一意のまま）ので枚数もトークンも増えない。**同じ番号が並ぶことになるので、その理由を画面に出す** | 1 |
| 115 | UI | 入力欄の未入力案内（placeholder）が本文と同じ色になっていないか。設定資料の名前欄は白のままで、**既に名前が入っているように見えて直す手が止まる**。入力欄ごとに書くと足し忘れるので、囲みの単位で1か所（`.rs-window input::placeholder`）に当てる | 1 |
| 116 | UI | 一覧を「+N」で打ち切っていないか。キャンバスのチップは4枚までで残りを`+3`と数だけ出しており、**何が付いているのかを見るには結局コマを選び直すことになり**、キャンバスに出す意味が無くなっていた。場所が足りないなら打ち切るのではなく**入れ物の幅で折り返す**。折り返し幅を渡すとき、**中身を倍率の逆数で戻している要素では画面上の大きさへ換算する**（換算しないと拡大するほど折り返しが早まる） | 1 |
| 117 | 反映 | JS/CSSを直したら`index.html`の`?v=`を上げたか。**このプロジェクトはファイルごとに版を振ってキャッシュを切っている**（`?v=7.2`〜`?v=8.0`が混在）。上げ忘れると、直した本人の環境では動いて見えても**利用者のブラウザは古いファイルを読み続け、「直っていない」という指摘だけが返ってくる**。**service-workerが`.css`/`.js`をキャッシュ優先で返すため、普通の再読み込みでも古いままになる**（→`llm_doc/project-structure.md`）。修正したファイルを`git status`で洗い出し、`index.html`の該当行を全部上げる | 1 |
| 118 | UI | 囲みや区切りの線を、**その下地の上で実際に見えるか**確かめたか。キャンバスのチップは枠線を`--color-accent`（#810000）の1pxで引いており、囲んでいるつもりで何も見えていなかった。**変数名が合っているかではなく、置いた場所での明度差で選ぶ。** ただし**原稿の上に出すものを目立つ色で描かない**（赤い2px線は絵より目立つ）。下地が白にも黒にもなるなら、**中間のグレーの細線**（どちらにも明度差が出る）にし、塗りは輪郭を助ける程度に留める。**濃い塗りは絵に膜が掛かって見える** | 3 |
| 119 | UI | 意味を持つ囲みの外側に、**入れ物の囲み**を重ねていないか。キャンバスのチップは自分の枠と下地を持ったまま中に組の枠を描いており、枠が二重になってどちらが関係の印なのか読めなかった。**意味を持つ方だけを残す。** 入れ物を透明にするときは当たり判定（`pointer-events`）を中身へ移し、隙間は下へ通す | 1 |
| 120 | CSS | 要素の見た目を変えたとき、**同じ要素の別の状態（`:hover` / `:focus` / `:active`）も見直したか**。キャンバスのチップは下地を消したのに`:hover`の`background:rgba(129,0,0,0.9)`が残り、載せた瞬間だけ外側の囲みが復活して二重に見え、色もきつかった。**状態側のルールは消し忘れても静止画では気づけない。** 見た目を変えたセレクタを`grep`して全状態を確認する | 1 |
| 121 | CSS | 並びの中で**囲みが付く要素と付かない要素**を混ぜるとき、付かない側にも同じ余白・同じ太さの透明な線を持たせたか。枠付きだけが`border+padding`分ふくらみ、囲みの無い画像が上へずれて並びが崩れていた | 1 |
| 122 | 品質 | アルゴリズムで描いた線を、**機械的に見えないか確かめたか**。吹き出しの輪郭は線幅一定で描いており、形をどれだけ作り込んでもベクター図形に見えていた。線ではなく**太さの変わる帯を面で塗り**、筆圧・下側が太い・曲率・インクの補充・内外エッジの独立ノイズを掛け合わせる。**振幅を上げても手描きにはならない**（決めているのは相関の構造）。さらに**強い不均一は「不気味な声」という意味を持つ**ので、通常の種別では抑え、大きな不均一は専用の種別に切り出す | 1 |
| 125 | 採寸 | 文字の大きさを測るとき、**描画側のライブラリが掛ける係数を入れたか**。Fabric は行の高さを `fontSize × lineHeight × 1.13`（`_fontSizeMult`）で計算する。採寸に入れないと文字が13%高くなり、実測で96通り中48通りが吹き出しからはみ出していた。**採寸と描画で同じ値を使う**（片方だけ直すと必ずずれる） | 1 |
| 126 | 組版 | 日本語を**決まった文字数で機械的に折っていないか**。文の途中で切れて素人の組版に見える。句読点・助詞のあとを優先する（文節優先）。ただし**文節優先だけだと行の長さが不揃いになり、いちばん長い行に合わせて入れ物が膨らむ**ので、目標文字数を振って何通りか折り、最も揃うものを選ぶ | 1 |
| 127 | 根拠 | 「漫画ではこうする」と書いた数値に**出典があるか確かめたか**。しっぽの長さを「本体半径の3〜7割」とコメントに書いたが裏が取れず、出典から言えるのは「先を口元に向ける」までだった。**未検証と分かった時点でコメントをそう書き換える**（→CLAUDE.md「検証していない仕組みを設計の根拠にしない」） | 1 |
| 128 | 根拠 | 記号の**意味**を取り違えていないか。爆発型の吹き出しに二重線を付けていたが、二重線の意味は日本で「客観視・回想」、英米で「叫びではない強調」で、**どちらも衝撃ではなかった**。形が似ているからと別の記号の装飾を借りると、読者には違う意味で伝わる。**形を足す前に、その形が何を意味するかの出典を取る** | 1 |
| 130 | ロジック | 「直近の1つ」を選ぶべき場面で、**全候補の最小値／最大値を取っていないか**。インク切れの計算で全補充点の最小を取っており、過去の補充の「切れ切った値」が常に効いて線がほぼ消えていた。周期的に回復する量は**直近の起点からの経過**で決める | 1 |
| 131 | 手順 | 1つのスクリプトで複数の置換をまとめて行うとき、**途中で失敗したら前半の置換も書き込まれない**（またはファイルごとに書き込み済みで不整合になる）ことを意識したか。置換が2回に分かれて片方だけ適用され、`lightningSide is not defined` で実行時に落ちた。**まとめて置換したら、対象の識別子を grep して全部入ったか確認する** | 1 |
| 133 | 検証 | 「0件」と書いた検査の**範囲**を書いたか。前回「帯の自己交差 0件」と報告していたが、実際に見ていたのは本体の輪郭だけで、**帯（内外オフセット）とフラッシュの多角形は検査していなかった**。範囲を広げたら 66,096通り中 3,915件出た。**検査したものと、していないものを対で書く。数字だけ書くと、次に読む人（自分を含む）は全部見たと受け取る** | 1 |
| 135 | 設計 | 種別ごとの係数表を作る前に、**測って出せないか考えたか**。「この形は文字の何倍に膨らませるか」を17種ぶん表に書いていたが、値に出典が無く、種別を足すたびに勘で1行増える。**その形を実際に作って、文字の矩形が入る最小の外形を測れば表は要らない**（`fitBody`）。測る側と描く側で seed・線の太さを揃えること | 1 |
| 136 | 設計 | 記号を**種別に焼き込んでいないか**。しっぽ無し・二重線・フラッシュ・連結は「回想でも枠なしでも使う」直交した記号なのに、種別の属性として持っていたため組み合わせが作れなかった。**種別は既定値だけを持ち、呼び出し側が上書きできるようにする** | 1 |
| 144 | 性能・品質 | **同じ量に上限が2箇所から掛かっていないか**。帯の幅に「曲率半径による上限」と「隣辺長の45%上限」の両方が掛かっており、点の間隔が細かい輪郭では後者が先に効いて、**指定した線幅より細い帯が黙って出ていた**（線幅10で要求の55%）。しかも見た目は「線が少し細い」だけなので気づけない。**要求値と実測値の比を測る** | 1 |
| 146 | 契約 | 「できなかったときは印を返す」と書いた関数で、**全ての失敗経路に印を立てたか**。しっぽを付けられない経路のうち、印を立てていたのは交差判定だけで、目標が近すぎる場合などは呼び出し側が「付けた」と誤認していた。**早期 return を全部数えて確認する** | 1 |
| 151 | 契約 | 「印を返す」実装が、**経路ごとに別の関数を通っていないか**。しっぽの省略印は片方の生成関数の結果からしか立たず、もう一方（丸を並べる経路）では構造上必ず false だった。**同じ意味の印は、経路の数だけ立てる場所がある** | 1 |
| 152 | fallback | 引数が壊れているとき、**黙って別の答えを返していないか**。`stroke` が undefined だと `clamp` を素通りして NaN が伝播し、採寸せずに入力をそのまま返していた（必要な2.22倍に対して1.015倍）。例外は出ないので気づけない。**公開する関数は引数を検証して例外にする。既定値で補うのはフォールバック** | 1 |
| 153 | fallback | `a||既定値` で**明示の 0 を握り潰していないか**。`stroke:0` が未指定と同じ扱いになっていた。`undefined`/`null` を明示的に判定する | 1 |
| 154 | 探索 | 目標値を探すループで、**「初めて超えた値」を採っていないか**。輪郭の山を跨ぐと測定値が不連続に跳ぶので、行き過ぎて逆向きに外れる。**目標にいちばん近かった値を採る**（行き過ぎ側・不足側のどちらでも）ようにすると、範囲外が 4.9%→2.4% に減った | 1 |
| 155 | fallback | 反復が**収束しなかったときに黙って返していないか**。`fitBody` は最大回数まで広げても入らない大きさをそのまま返しており、呼び出し側は「測った」と受け取ってそのまま文字を置いていた。収束判定を最後の測定値で行い、収束しなければ例外にする | 1 |
| 156 | 手順 | 大きな範囲を一括で置換するとき、**置換の始点と終点の間に別の関数が挟まっていないか確認したか**。「コメント行から次の関数定義まで」で切ったつもりが、間にあった5つの関数（`tailCrossesBody` `buildBodyWithTail` `makeTail` `tangentAt` `offsetPolygon`）ごと消えた。構文チェックは通り、実行して初めて `is not defined` で気づいた。**置換前に、消える範囲に含まれる関数名を列挙して確認する** | 1 |
| 160 | 検証 | 平均や合計で見て**局所の破綻を見落としていないか**。帯の実効幅は「面積÷長さ」で 0.91 と良好だったが、局所では線幅5に対して56pxの塊ができていた。**隣り合う点の差**や**最大値**も見る | 1 |
| 162 | 手順 | 一括置換で**同じ文字列が複数行に現れるとき、意図した行だけに当たったか確認したか**。`edge:0.12` を `0.24` にする置換が、意図した種別の1つ前の行にも当たり、ノイズ音声のエッジノイズが倍になっていた（見た目は「線が少し荒い」だけなので気づけない） | 1 |
| 169 | 症状 | 「線が太いほど目立つ」という症状は、**幅そのものではなく幅の変化率**を疑う。太さに比例して増えるのは、幅に掛かる係数の変動が幅倍されて出ているため | 1 |
| 170 | 履歴 | 読み取りだけのはずの描画処理で`set()`を呼んでいないか。レイヤーパネルのサムネイルは`toCanvasElement()`が内部で`setPositionByOrigin()`→`set('left')`を実行し、自動コミット網（`_set`フック）が「キャンバスが変わった」と誤検知する。`updateLayerPanel()`は履歴の保存後と復元後に毎回走るため、Undo直後に次のキー操作・クリックで空のコミットが積まれ、`captureState()`の`splice(currentStateIndex+1)`でRedoが丸ごと消える。キャンバスを変えない描画は`withoutHistory()`で囲む | 1 |
| 171 | 履歴 | 復元後のキャンバスが**保存時と同じJSONにシリアライズされるか**。`saveInitialState()`は`obj.clipPath.initial`に`strokeWidth`を入れず、復元後に`resetEventHandlers()`→`updateClipPath()`が作り直したclipPathには入る、のように経路ごとに形が違うと1プロパティの差でも`captureState()`の重複除去をすり抜ける。すり抜けた瞬間にRedoが切り捨てられ、Undoは同じ位置を往復して進まなくなる。同じ構造体を作る箇所が複数あるならキーを揃える | 1 |
| 172 | 手順 | 同じ意味の値が**2箇所**にあって、外側が内側の既定値を上書きしていないか。`rootFlare` を関数の既定値で直したが、呼び出し側の `tailOpt` にも同じキーがあり、**直したつもりの分岐が死にコード**だった。総当たりで数字が1つも動かないのに気づいて発覚した。**数字が動かない修正は、まず「効いているか」を疑う** | 1 |
| 181 | 契約 | 「省略した」と印を立てながら、**一部を描いていないか**。心の声のしっぽは丸が1個のとき「省略」の印を立てたうえで1個描いており、画面には丸がぽつんと残った（実測: 省略124件のうち89件=72%）。省略するなら描かない | 2 |
| 182 | 検証 | 計測スクリプトで**文字列置換が一致しなかったとき、黙って素通り**していないか。`String.replace` は一致しなければ何もしないので、「全条件で差が出ない」「全部0」という無害に見える結果になる。**置換後に結果が変わったことを assert する**（今回2回踏んだ） | 1 |
| 186 | 検証 | 下流でまとめて掃除する処理を足すとき、**上流で意図的に作ったものまで消していないか**。退化した辺を落とす処理を全体に掛けたら、しっぽの根元にわざと集めた点まで巻き添えで消え、根元の折れ角が 6.4度 → 13.0度に戻った。**作る側で作らない**方が漏れない | 1 |
| 188 | 検証 | 「なめらかになった」という数字を、**量そのものが縮んでいないか**と対で見たか。帯の縁の出っ張りは線を細くするだけで劇的に良くなる。実測で電子音声が 0.3px超 24%→9% になったが、同時に線幅が指定の 0.36倍（線幅10指定で 3.6px）まで落ちていた。**要求値と実測値の比を必ず並べる** | 1 |
| 194 | 契約 | 破綻を避ける手段が2つあるとき、**黙って出るものと印で返せるものの、どちらを選ぶか意識したか**。しっぽのすきまが線幅を下回るケースは「塗りの塊が黙って出る」か「しっぽを省略して印を返す」かの二択で、後者を選んだ（省略 3.5%→5.4% と引き換えに 20px²超のループが 5件→0件）。**呼び出し側が気づけるほうを選ぶ** | 1 |
| 197 | 設計 | 値の調整で消えない仕組みを、**外す判断をしたか**。紙の目のノイズは、コブが消える振幅では効果が画素より小さく、効く振幅では必ずコブになる。**効かないつまみを残すより外す**。外したことと理由をコードに書き残す | 1 |
| 199 | 検証 | 計測スクリプトが**必須の引数を渡しているか**。`o.x`/`o.y` を省いたら `undefined + 数値` で全点が NaN になり、「全種類で描画が壊れている」という誤った結論を出しかけた。**まず自分の計測側を疑う** | 1 |
| 200 | 手順 | Python の `io.open(path,'w')` は Windows で **改行を LF から CRLF へ黙って変換する**。LF のファイルを1行直しただけで全行が CRLF に変わる。編集後に混在していないか確かめるか、`newline=''` を渡す | 1 |
| 203 | 契約 | **何も描かないのが正しい状態**を、画面と返り値の両方で見分けられるか。枠なし（モノローグ）はレイヤー0件が正しいが、壊れて0件なのと区別が付かなかった。検証用の画面では「描かないのが正しい」ことを明示する | 1 |
| 204 | 契約 | 早期 `return` の手前で、**直交するはずの機能まで落としていないか**。枠なしはコメントに「フラッシュはどの種別にも掛けられる」と書いてあるのに、その行の手前で `return` していたためフラッシュ・白フチ・二重線が黙って消え、`doubleDropped` は返り値にすら入っていなかった。**早期 return の分岐は、返り値のキーが本流と揃っているかを確かめる** | 1 |
| 205 | 検証 | 直せと言われた症状が**その修正で直るのかを、先に確かめたか**。「小声が小声に見えない」に対し、外した機構を戻す案を4つ並べて描いたが、**4つとも同じ絵**だった。原因は別（切れの深さ）で、戻す作業は無駄になるところだった | 1 |
| 206 | UI | **効いていない経路で数字だけ出していないか。** 設定資料は参照画像に対応していないサービス（ComfyUI等）では1枚も送られないのに、画面は「送る画像 3 / 14」「トークン ~1,548」を出し続けていた。数字は「効いている」と読まれる。生成しても資料は絵に出ないので、出さなければ最後まで気づけない。**送られないなら数字を数字のまま出さない**（取り消し線）＋理由を出す | 1 |
| 207 | 設計 | **対応可否を「登録されているか」で判定していないか。** `providerRegistry.get('googleImage')`はプロバイダーが登録さえされていれば常に返るため、ロール割り当てがComfyUIでも警告が素通りしていた。見るのは`getProviderForRole(role)`。対応の有無は基底クラスの能力（`supportsReferenceSheets()`、既定`false`）1か所に持たせる。対応サービス名も`getAll()`から数え上げる（文言に書き並べると増えたときそこだけ古くなる） | 1 |
| 208 | UI | **警告文に塗り用の色トークンを使っていないか。** `--color-accent`（#810000）は枠と塗りの色で、暗い背景の上の文字としては読めず、注意書きが「潰れている」ようにしか見えなかった。文字には`--btn-danger-color`（テーマ両方に値がある） | 1 |
| 209 | UI | **作業ウインドウの入口を「設定の棚」だけに置いていないか。** 設定資料は使用サービス・モデル設定と同じ行に並んでいたが、実際は1コマずつ何十回も開くもの。**対象を持つ側（右クリックメニュー・レイヤーパネルのバッジ）から開ける経路を必ず残す。** バッジは数だけ出して押せないと、直したいコマが分かってから設定まで目を戻すことになる | 1 |
| 210 | UI | **空の状態に手順だけ書いていないか。** 「コマをクリックすると割り当てられます」は操作手順で、資料を入れると何が変わるのかが1文も無かった。**手順の前に「何のための箱か」を置く。** 同じことが枠（関係）にも当てはまり、「関係します」だけでは何がどう送られるのか読めない → 実際に送られる1行をそのまま出す | 1 |
| 211 | 手順 | **実アプリで検証するとき、自動保存に書き込んでいないか。** 復元ダイアログから利用者のプロジェクトを開いて検証データを足したところ、タイマーの自動保存が走って利用者の自動保存（別日付）を上書きした。スロットは1つで、古い方は戻らない。**検証を始める前に自動保存を止める**（`AutoSaveManager.stop()`）。IndexedDBへ足したものも必ず消す | 1 |
| 212 | i18n | **日付キーを重複させていないか。** `const resources`のブロックキーが同じ日付だと、JSのオブジェクトリテラルは後勝ちで前のブロックが丸ごと消える。`"20260803120000_001"`が2回あり、13キー×8言語＝104件の翻訳が読み込み時に消滅し、「どんな漫画か」の選択肢が全言語で`storyToneGeneral`等のキー名のまま表示されていた。**追加時は既存キーとの重複をgrepで確かめる** | 1 |
| 213 | i18n | **どの言語にも定義が無いキーを見逃していないか。** 言語間の突き合わせ（`npm run check-translations`）は全言語に無いキーを検出できない。`russian`/`spanish`/`Germany`が未定義で、言語切替メニュー自体が生のキー名を表示していた。`i18next.init`に`missingKeyHandler`を入れて実行時に拾う | 1 |
| 214 | i18n | **`getText('x') \|\| 'デフォルト'`を安全網だと思っていないか。** `getText()`は未定義でもキー文字列を返すため戻り値が falsy にならず、右辺は一度も評価されない。44箇所すべてが死んでいた。`updateContent()`の未翻訳warnも同じ理由で一度も出ていなかった | 1 |
| 215 | UI | **`button.textContent=`で状態を書き換えていないか。** 子要素（アイコン`<i>`と`data-i18n`付き`<span>`）が丸ごと破棄される。ナイフボタンは初回クリックで鋏アイコンが消え、以降は言語切替でも更新されなくなっていた。**`querySelector('span').textContent`に書き、`data-i18n`も張り替える** | 1 |
| 216 | UI | **トグルのラベルを「押すとどうなるか」で書いていないか。** ナイフはON中に「ナイフOFF」と表示しており、今どちらなのか読めなかった。**ラベルは状態を出し、切り替わることは`.selected`の見た目で示す** | 1 |
| 217 | AI | **キューの取消が実行中のタスクに効いているか。** `cancelAiTask()`は待機中しか落としておらず、実行中はインジケータを消すだけでPromiseが生き残り、取り消したはずの画像がキャンバスに配置され課金も実績記録も走っていた（中断信号を送るのはComfyUIのみ）。**待機中・実行中の入口を`cancelItem()`1つに統一する。** なおリモート側の処理と課金は止まらない（AbortController未対応） | 1 |
| 218 | 保存 | **元に戻せない操作に確認を置いているか。** ページ削除(🗑)はページ切替クリックの至近距離にあり、Undoはページ単位なので押した瞬間に復旧手段が無くなる。「全て削除」も`stateStack=[]`で履歴ごと消す。**確認ダイアログは各所でベタ書きせず1つにまとめる**（×・Esc・背景クリックの閉じ方が揃わなくなる） | 1 |
| 219 | 保存 | **未保存のまま離脱できてしまわないか。** `beforeunload`に警告が無く、未保存表示も皆無だった。判定を「変更イベントの回数」で作ると起動時やページ切替の内部`captureState()`まで拾って誤検知する。**保存時点の内容そのものを控えて突き合わせる。** その際`canvasGuid`はページ識別子で中身ではないので比較から外す | 1 |
| 220 | UI | **通知が読み終える前に消えていないか。** エラートースト112件中62件が既定4秒で、×ボタンもホバー停止も無かった。生のHTTPステータスと応答本文を出すエラーほど読む時間が要る。**`startProgressBar`1か所にホバー停止と×を入れる** | 1 |
| 221 | UI | **アイコンのみのボタンにアクセシブル名があるか。** `<i class="material-icons">swap_horiz</i>`はリガチャ文字がそのまま読み上げられる。index.htmlの`title`は0件、`aria-label`は1件だった。**ツールチップ登録関数（`addTooltipByElement`）の中で`aria-label`も付ける。** 個々のボタンに手で足すと必ず抜ける | 1 |
| 222 | UI | **フォーカスの当たり位置が見えるか。** `button:focus`の定義が0件、`outline:none`が代替なしで11箇所あった。**`button{}`の直後に`:focus-visible`を1つ置けば全ボタンに効く** | 1 |
| 223 | UI | **同じアイコンで意味が変わる場面を放置していないか。** 縦書きでは整列3ボタンが上/中央/下として働くのに、アイコンは`format_align_left`のままでツールチップも無く、押して結果を見るまで分からなかった。**表示の切替は`updateTextAlignUI()`のような反映処理1か所で行う** | 1 |
| 224 | 手順 | **Service Workerのキャッシュを考えたか。** `.js`は`?v=x.y`が変わる前提でcache-first配信される。中身だけ変えて`?v=`を据え置くと、既存利用者には旧版が配られ続ける。**まとめて直したときは`CACHE_VERSION`を上げる。** 検証時もSW登録とcachesを消さないと古いJSを見続ける（実際に修正が反映されず原因を誤認しかけた） | 1 |
| 225 | 検証 | **未定義キーの検知は実行時にしか出ないものがある。** `missingKeyHandler`を入れた直後に`SvgDownload`/`Glow`/`GLFX`の3件が出た。いずれも静的チェックでは検出できず、画面にはキー名がそのまま出ていた。**仕組みを入れたら、その場でログを読んで拾い切る** | 1 |
| 226 | 検証 | **「効くはず」で終わらせず、効いた量を測ったか。** ブレ線は実装され例外も出ないのに、振れ幅が本体の帯の 0.73倍止まりで **seed 200本すべてで一度もはみ出していなかった**（＝常に見えない）。ペンの筆致も帯の輪郭が最大 0.55px しか動かない。**「レイヤーが増えた」「出力が変わった」は動いた証拠にならない。画素として見える量まで測る** | 1 |
| 227 | 検証 | **一覧が縮小表示になっていないか。** 幅3570pxのcanvasを`max-width:100%`で約4割に縮めていたため、1px未満の差（ブレ線・筆致・インクだまり）が**原理的に見えなかった**。効きが小さい機能を「効かない」と誤認する。**表示倍率を明示し、既定を100%にする** | 1 |
| 228 | 検証 | **白い物を白い背景で確認していないか。** 「地を塗らない」は白い塗りを消すだけなので、白い紙の上では画素が1つも変わらない。実装は正しいのに「動かないつまみ」に見えていた。**確認用の背景（格子・濃淡）を用意する** | 1 |
| 229 | 設計 | **0 を「未指定」の合図に使っていないか。** `style.dips\|\|(乱数)`と書いていたため、`dips:0`（インク切れを作らない＝マーカーの一番の特徴）が falsy 判定で乱数に戻り、**黙って消えていた**。**未指定は`undefined`で表し、`===undefined`で判定する** | 1 |
| 230 | 設計 | **つまみが単調に効くか確かめたか。** 花形の`valley`は 0.85 付近が最深で、下げても上げても浅くなっていた（実測 0.40→0.858 / 0.85→0.698 / 1.00→0.809）。閉じた式の解が成り立つ範囲を外れていたため。**「深くしよう」と下げたら逆に浅くなるつまみは、無いより悪い** | 1 |
| 231 | 設計 | **上限や制約を、向きを考えずに絶対値で掛けていないか。** 二重線の内周は「内側へのオフセット」なので折り返すのは**凹み**だけなのに、曲率の絶対値で上限を取っていた。凸の角まで押さえてしまい、直角の四角では二重線が seed 20通り**すべて**落ちていた | 1 |
| 232 | 設計 | **同じ点を重ねて置いていないか。** 角丸の半径 0 でも分割数ぶん（7点）同じ位置に積んでおり、長さ 0 の辺が四隅に並んで曲率が符号ごと壊れていた。上の 231 と重なって「直角の四角に二重線が引けない」になっていた | 1 |
| 233 | 設計 | **一部だけ上書きして「効いた」ことにしていないか。** 稲妻しっぽの先広がりは中心線8点のうち**最後の1点だけ**を上書きしており、ラッパにならず1辺だけのトゲになっていた。しかも先端で幅が 5.1px→0.74px と1点で飛ぶ段差でもあった。**端点を上書きするのではなく、根元から先端まで補間する** | 1 |
| 234 | 設計 | **記号の意味と実装の狙い先が一致しているか。** 稲妻しっぽは出典上「音源（機器）」を指す記号なのに、`TAIL_AIM`に項目が無く**顔（口元）**を指していた＝電話・放送・テレビの声が人の口から出ていた。**機器の位置は検出していないので推測で置かず、未指定ならしっぽを付けない** | 1 |
| 235 | 設計 | **早期returnの経路に、後から足した返り値を入れ忘れていないか。** 枠なし（モノローグ）の分岐が`fontScale`/`textSkew`を返しておらず、呼び出し側が`undefined.toFixed()`で落ちた。**返り値を足したら分岐の数だけ足す** | 1 |
| 236 | 設計 | **「付けなかった」印が実態と合っているか。** 枠なしは線を描かないのでしっぽも出ないのに`tailDropped:false`を返し、位置情報まで入れていた（呼び出し側は「付けた」と誤認）。`halo`/`doubleLine`/`underDraw`は正しく印が立っており、しっぽだけ抜けていた | 1 |
| 237 | 設計 | **下位の関数が引数を受け取っていないだけ、ということはないか。** 心の声の丸しっぽは`thoughtTail()`が`tailOpt`を引数に取っておらず、`voice`も`tailOpt`も`tailTip:'squink'`も**すべて黙って無視**していた。上位では「対応済み」に見える | 1 |
| 238 | 設計 | **点に付けた印が、下流で作り直されて消えていないか。** `bodyBetween`は点を`{x,y,wm}`で作り直すので、輪郭に付けた印は差し込みを通るたびに落ちる。**印で運ばず、出来上がった形の側で幾何的に見つける** | 1 |
| 239 | UI | **利用者が触るつまみが「場面」になっているか。** 17種別に対して「せん断」「台形」「先広がり」を並べても、利用者はそれを場面へ翻訳できない。実測すると必要な形の 8割近くは既に出せており、**足りなかったのは機能ではなく対応表**だった。「機能が足りない」と言われたら、まず出せる形を数える | 1 |
| 240 | fallback | **`typeof X==='function'` / `typeof X!=='undefined'` で存在確認して黙って素通りしていないか。** 未定義でも何も起きないため、読み込み順が壊れたときに「押したのに何も起きない」になり原因が追えない。`index.html`のscriptは全て`defer`で記述順に実行され、ユーザー操作時の呼び出しは全スクリプト実行後なのでガードは不要。直接呼ぶ  | 1 |
| 241 | 翻訳 | **翻訳が書き込む属性を、表示用の値で上書きしていないか。** `data-label`をi18nが訳で埋めるのに、スライダーが同じ属性へ`ラベル：値`を書き戻すと訳が壊れ、以後englishのまま固定される（初回起動が`savedLanguage="en"`のため英語を掴む）。原文と表示値は別の属性に分け、値の連結はCSSの`content: attr(...) attr(...)`で行う  | 1 |
| 242 | 構文 | **`node --check`が通らない時、まずトップレベル関数の二重定義を疑う。** `package.json`に`"type":"module"`があるためESMとして検査され、classic scriptなら後勝ちで通る二重定義が落ちる。ブラウザ側は動くので気付きにくい  | 1 |
| 243 | モード | **モードのAPIを呼んだ後に自前のフラグをトグルしていないか。** `ModeManager.clearAll()`が内部で`poly.edit=false`にした後に`poly.edit=!poly.edit`すると`true`へ戻り、モードから抜けられなくなる。`var willEdit=!poly.edit;`のように先に確定させてからAPIを呼ぶ  | 1 |
| 244 | モード | **全オブジェクトの`selectable`等を一律に書き換えて元の値を捨てていないか。** モードを抜けた瞬間にロックされていたコマまで動くようになる。`ModeManager.lock.apply(props)`/`restore()`で書き換えるプロパティだけを退避する。モード中に増えたオブジェクト（分割で生まれたコマ等）は退避も継承しないと兄弟と食い違う。`excludeFromLayerPanel`の除外は`lock`側の1か所で持つ  | 1 |
| 245 | UI | **「ロックが既定」なのに、ロックされていることが画面から読めるか。** コマは`selectable:false`で生まれるのにカーソルも案内もトーストも無く、レイヤー行だけ赤くなる半端な選択状態になっていた。既定で操作を止めるなら、止まっている理由と解除の入口を同じ画面に出す  | 1 |
| 246 | 設計 | **状態が「保存された時」にしか登録されない作りになっていないか。** ページは`btmSaveProjectFile()`が走った時だけ`btmProjectsMap`に載るため、追加直後・起動直後は一覧に無く、番号・前後移動・存在判定がそこから全部狂い、空のまま移動すると消えていた。作られた時点で登録する  | 1 |
| 247 | 設計 | **取り消せない操作を、既定の選択肢や副作用として実行していないか。** 自動保存の復旧ダイアログは「復元しない」を押した瞬間にIndexedDBを消し、プロジェクト保存は成功時に自動保存データを消していた。消す操作は明示的な選択にし、確認を通す  | 1 |
| 248 | UI | **起動時に出すモーダルの順番が1か所で決まっているか。** `TutorialManager.init`と`AutoSaveManager.init`が互いを知らずにモーダルを出し、言語選択の上に復旧ダイアログが被さって言語ボタンが押せなくなっていた。相互参照や待ち合わせ判定を各所に撒かず、起動シーケンス1か所で直列に流す。またオーバーレイはポインタしか塞がず`document`の`keydown`は通る点に注意  | 1 |
| 249 | 検証 | **grepの結果を根拠に不具合だと断定していないか。** そのgrepが対象の全件に当たるかを先に数えて確かめる。`^"20[0-9]{12}_[0-9]{3}":\{`は`": {"`のように空白を挟む書き方に当たらず、59ブロック中48件しか見ていなかった。逆に`:\{`を外すと同じ命名規則の**翻訳キーまで拾って重複に見える**。件数を数える・実際の該当行を目で見る、のどちらかを通してから報告する  | 1 |
| 250 | 検証 | **UIを直したら、アプリを開いて見たか。** Inpaintのマスクは実機で生成して直ったことを確かめた一方、ワークフロー設定画面（Type一覧・モデル名の警告表示）は**一度も開かずに「完了」と報告していた**。コードとNodeでの評価だけでは、描画順・CSS・イベントの結び付きは分からない。`navigate`ツールは`file://`へ遷移できない（→`llm_doc/chrome.md`）ので、**UIを触ったタスクは、着手時点でユーザーにタブを開いてもらう段取りを入れる**。開けないまま終えるなら、報告に「画面は見ていない」と明記する  | 1 |

## 分離したもの

吹き出し輪郭の生成（スーパー楕円・オフセット多角形・ノイズ・しっぽ）に固有の項目は
`llm_doc/balloon-geometry.md` へ移した。その機能を触るときだけ読む。
